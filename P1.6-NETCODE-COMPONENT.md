# P1.6 可复用网络预测组件（边界与 API）

> `P1.5` 定的是**算法**（预测 / 校验和解 / 重放 / 插值 / 外推）。
> 本文定的是**组件边界**：把它做成一个能复用、能单测、能脱离本作存在的东西。
>
> 目标形态：**引擎无关的核心库 + 极薄的接入层**。
> 核心库不知道 Godot、不知道 protobuf、不知道地形/坡度/ORCA、不读系统时钟。

---

## 1. 依赖规则（硬约束，决定它能不能复用）

| 规则 | 为什么 |
| --- | --- |
| **零 Godot 引用** | 核心库要能在无引擎环境下跑单测，也能给别的引擎/服务端工具复用 |
| **零协议引用**（不碰 protobuf、不碰 `WorldService`） | 组件只认"tick + 状态 + 输入"，谁喂的、怎么解出来的与它无关 |
| **零游戏类型引用**（不碰 `TileMap`/`SlopeSpeed`/`OrcaAvoidance`） | 那些是**游戏侧注入的模型**，不是组件的知识 |
| **时间全部注入** | 所有 API 都收 `nowMs`/`tick` 参数；组件内部不许调 `DateTime.Now`/`Time.GetTicksMsec` → 测试可用合成时间精确复现 |
| **热路径零分配** | 输入历史/快照缓冲都是定长环形缓冲；诊断是只读 struct |
| **泛型而非硬编码 `Vector2`** | 状态类型由游戏决定（我们的移动状态是"锚点+子格+速度"，不只是坐标） |

> 结论：组件可以放在本仓库里做独立程序集，也可以在需要时**原样搬出去**（改命名空间即可）。

---

## 2. 模块划分

```
┌──────────────────────────── Starve.Netcode（引擎无关，纯 C#）────────────────────────────┐
│                                                                                          │
│  ServerClock              墙钟 ↔ 服务端 tick 的稳定映射（慢速校正，不跳变）                 │
│                                                                                          │
│  InputHistory<TInput>     输入历史环形缓冲：seq/epoch/生效tick/输入                         │
│                           · Record / Acknowledge / IntentAtTick                          │
│                                                                                          │
│  PredictedEntity<TState,TInput>   ← 自己的那条路                                          │
│                           · SetIntent / AdvanceTo / Reconcile / LastReport               │
│                                                                                          │
│  SnapshotBuffer<TState>   快照环形缓冲（按 tick 存状态 [+ 可选权威速度]）                    │
│  RemoteEntity<TState>      ← 别人的那条路：播放时钟 → 插值 → 有界外推                       │
│                                                                                          │
│  INetCodeModel<TState,TInput>   ← 游戏侧必须提供的东西（唯一的耦合点）                      │
│  NetcodeConfig / 各种 Report    ← 可调参数 + 只读诊断                                      │
└──────────────────────────────────────────────────────────────────────────────────────────┘
        ▲ 注入模型 + 喂 tick/状态/输入                          │ 读状态 + 读 Report
        │                                                      ▼
┌───────┴───────────────────────────┐        ┌───────────────────────────────────────────┐
│ Starve.Core（游戏数学，留在原处）   │        │ GodotClient（接入层，极薄）                 │
│ · OwnMovementSim ⇒ 实现 INetCodeModel│        │ · 每实体一个 RemoteEntity + 自己一个        │
│ · SlopeSpeed / MovementSlide / ORCA │        │   PredictedEntity                          │
│ · TileMap / walkable                │        │ · 从 WorldService 喂数据；渲染层读结果      │
│ （movement_golden.json 守同构契约）  │        │ · MoveTrace 直接读组件的 Report 落日志      │
└─────────────────────────────────────┘        └───────────────────────────────────────────┘
```

**唯一耦合点**就是 `INetCodeModel`。换一个游戏 = 换一个模型实现，组件一行不改。

---

## 3. 游戏侧必须提供的东西

```csharp
public interface INetCodeModel<TState, TInput>
    where TState : struct
    where TInput : struct
{
    /// 一步推进，dt 固定 = 一个 tick。**必须与服务端同一套数学**（我们由 golden 向量保证）。
    void Step(ref TState state, in TInput input, double dtSeconds);

    /// 把权威状态写进本地状态（重放的起点）。
    void Rebase(ref TState state, in TState authoritative);

    /// 状态间距离（用于误差判据、传送阈值）。单位随意，只要与配置里的阈值一致。
    float Distance(in TState a, in TState b);

    /// 别人用的插值。
    TState Lerp(in TState a, in TState b, float t);

    /// 可选：由状态取"表现层速度"（驱动动画/朝向）。不需要就返回 false。
    bool TryGetVelocity(in TState state, out float vx, out float vy);
}
```

> 注意 `Step` 的**输入只有 state + input**：世界查询（地形可走、静态障碍、动态邻居、
> 当前坡度）由游戏侧在调用前**注入到模型实例**里（我们就是 `SetBlockers`/`SetNeighbors`/
> `HeightAt`）。这样状态本身是可拷贝、可回滚、可重放的纯数据。

### 我们的实现（`Starve.Core`）

把 `OwnMovementSim` 拆成两半：

| 部分 | 归属 | 说明 |
| --- | --- | --- |
| `OwnMoveState` | 游戏侧 | `{ int AnchorX, AnchorY; float SubX, SubY; float VelX, VelY; }` —— 可拷贝的纯数据 |
| `OwnMoveModel : INetCodeModel<OwnMoveState, MoveIntent>` | 游戏侧 | 现有的 `Tick` 数学（坡度 → 静滑 → ORCA → 格子层）＋ `SetBlockers/SetNeighbors/HeightAt` 作为世界注入 |
| 预测/和解/重放/时间 | **组件** | 从 `OwnMovementSim` 里搬去 `PredictedEntity` |

---

## 4. 公开 API 草案

```csharp
namespace Starve.Netcode;

// ── 时间基准 ────────────────────────────────────────────────
public sealed class ServerClock
{
    public ServerClock(NetcodeConfig cfg);
    public void OnSnapshot(long serverTick, long nowMs);  // 慢速校正；偏差过大才硬同步
    public double TickAt(long nowMs);                     // 当前服务端 tick（含小数）
    public double LeadTicks(long nowMs, long snapshotTick);
    public bool HardResynced { get; }
}

// ── 输入历史 ────────────────────────────────────────────────
public sealed class InputHistory<TInput> where TInput : struct
{
    public InputHistory(int capacity = 64);
    // effectiveTick = clock.TickAt(发送时刻)，由调用方算好传进来（组件不依赖时钟）
    public void Record(ulong seq, ulong epoch, double effectiveTick, in TInput input);
    public void Acknowledge(ulong appliedSeq);            // 丢弃 <= appliedSeq
    public TInput IntentAtTick(double tick);              // 最后一条 effectiveTick <= tick
    public int Count { get; }
    public ulong LatestSeq { get; }
}

// ── 自己：预测 / 和解 / 重放 ─────────────────────────────────
public sealed class PredictedEntity<TState, TInput>
    where TState : struct where TInput : struct
{
    public PredictedEntity(INetCodeModel<TState, TInput> model, NetcodeConfig cfg);

    public TState State { get; }
    public bool HasState { get; }
    public InputHistory<TInput> Inputs { get; }

    /// 正常帧：推进到 targetTick（内部按 tick 切片，从输入历史取"当时那条"意图）
    public void AdvanceTo(double targetTick);

    /// 收到权威快照：
    ///   Rebase(权威) → AdvanceTo(nowTick)  ← 后半句就是"重放"
    /// 无副作用：只改 state，不碰渲染/动画/音效/玩法。
    public ReconcileReport Reconcile(
        in TState authoritative,
        long snapshotTick,
        ulong appliedSeq,       // 服务端"已烘进这份位置"到第几号（缺则传 acceptedSeq）
        double nowTick,
        bool serverStopped);

    public ReconcileReport LastReport { get; }
}

// ── 别人：插值（+ 有界外推） ─────────────────────────────────
public sealed class RemoteEntity<TState> where TState : struct
{
    public RemoteEntity(INetCodeModel<TState, TInput> model, NetcodeConfig cfg); // 只用 Lerp
    public void Push(long tick, in TState state, float vx = float.NaN, float vy = float.NaN);
    public TState Sample(double nowTick);                 // 播放时钟采样
    public InterpReport LastReport { get; }
    public int Buffered { get; }
}

// ── 配置 ───────────────────────────────────────────────────
public sealed record NetcodeConfig
{
    public double TickMs { get; init; } = 50;                 // 服务端 tick 长度
    public double InterpolationDelayTicks { get; init; } = 2; // 别人看过去多少
    public double MaxExtrapolationTicks { get; init; } = 1;   // 外推上限（到顶冻结）
    public float  ReconcileDeadzone { get; init; } = 0.03f;   // 误差死区
    public float  TeleportDistance { get; init; } = 4f;       // 超过视为瞬移
    public double MaxReplayTicks { get; init; } = 12;         // 重放上限（保护）
    public double ClockMaxStepMs { get; init; } = 2;          // 时钟每次最多校正多少
    public double ClockResyncMs { get; init; } = 250;         // 超过就硬同步
    public bool   EnableReplay { get; init; } = true;         // 关掉 = legacy 行为（可回退）
    public bool   EnableExtrapolation { get; init; } = true;
}

// ── 诊断（只读 struct，直接喂日志） ───────────────────────────
public readonly record struct ReconcileReport(
    ReconcileKind Kind, long SnapshotTick, ulong AppliedSeq,
    float ErrX, float ErrY, float Err, float ImpliedLeadMs,
    float DriftX, float DriftY, float DriftSpeed,
    int ReplayedTicks, bool Teleport, bool Stale);

public readonly record struct InterpReport(
    double PlayoutTick, long FromTick, long ToTick, float Alpha,
    bool Extrapolating, bool Starved, int Buffered);
```

### 使用侧（GodotClient）大概长这样

```csharp
// 初始化
var cfg = new NetcodeConfig();
var clock = new ServerClock(cfg);
var me = new PredictedEntity<OwnMoveState, MoveIntent>(ownModel, cfg);
var remotes = new Dictionary<ulong, RemoteEntity<OwnMoveState>>();

// 每个快照到达（WorldService 喂进来）
clock.OnSnapshot(world.WorldTick, now);
for (每个实体 e) {
    if (e == 自己) {
        me.Inputs.Acknowledge(e.LastAppliedSeq);
        me.Reconcile(e.State, world.WorldTick, e.LastAppliedSeq, clock.TickAt(now), e.Stopped);
    } else {
        remotes[e.Id].Push(world.WorldTick, e.State, e.VelX, e.VelY);
    }
}

// 每帧
me.SetIntent(读输入(), seq, epoch, clock.TickAt(发送时刻))  // 只在意图变化时
me.AdvanceTo(clock.TickAt(now));
foreach (var r in remotes.Values) render(r.Sample(clock.TickAt(now)));   // 只有渲染读它
```

---

## 5. 组件**不**负责什么（写清楚，避免变成万能类）

- 不负责传输（TCP/UDP/WebSocket 都行）、不负责序列化；
- 不负责输入采样（键鼠 → 意图是应用层的事）；
- 不负责渲染、动画、音效、相机；
- 不负责玩法的权威判定（命中/伤害）—— **明确不做延迟补偿**；
- 不负责服务端：服务端只提供 `tick + 权威状态 + appliedSeq + 速度`。
  （可选：把同一份 `INetCodeModel` 在服务端也用起来，就天然同构了——但那是另一件事。）

---

## 6. 迁移路径（谁搬走、谁留下）

| 现状 | 去向 |
| --- | --- |
| `Starve.Core/PositionSmoother.cs` 的时间轴/插值/外推 | **搬**进组件 → `SnapshotBuffer` + `RemoteEntity` |
| `Starve.Core/OwnMovementSim.cs` 的预测/和解/重放/游标 | **搬**进组件 → `PredictedEntity`（+ `InputHistory`） |
| `Starve.Core/OwnMovementSim.cs` 的移动数学 | **留**在 `Starve.Core`，改造成 `OwnMoveModel implements INetCodeModel` |
| `Starve.Core/SlopeSpeed|MovementSlide|OrcaAvoidance|TileMap` | **留**（游戏公式） |
| `GodotClient` 的喂数据/读结果 | 改为调用组件；`MoveTrace` 改为读 `Report` |
| `Starve.Core.Tests` 的位置平滑/精度测试 | 拆两类：**组件自带测试**（无游戏依赖）+ 游戏侧的 golden 同构测试 |

拆完后 `Starve.Core` 里不再有"时间轴/缓冲/重放"这类横切逻辑，只剩游戏数学。

---

## 7. 测试策略

| 层 | 测试 | 依赖 |
| --- | --- | --- |
| **组件**（自带测试工程） | 时钟校正单调不跳；输入历史裁剪；重放确定性（喂录制的意图流 → 断言状态）；重放上限保护；外推有界/到顶冻结/不倒退；插值在样本间连续；死区/传送分类；`AdvanceTo` 时间不被消费两次 | 只需一个**假模型**（比如一维匀速 + 可选碰撞），不需要游戏 |
| **游戏侧** | `movement_golden.json` 同构（已有）；坡度/ORCA/格子层行为 | `Starve.Core` |
| **接入层** | `MoveTrace` 判据（`driftSpeed≈0`、`decision=NoError`、`dRendY` 无尖峰） | 真实客户端 |

关键：**组件测试用一个玩具模型就能覆盖全部机制**，这本身就是"可复用"的证明。

---

## 8. 复用清单：换一个游戏要改什么

1. 实现一个 `INetCodeModel<TState,TInput>`（该游戏的移动数学，与服务端同构）；
2. 按需调 `NetcodeConfig`（tick 长度、插值延迟、死区、是否外推/重放）；
3. 在快照到达处喂 `Reconcile` / `Push`，每帧喂 `AdvanceTo` / `Sample`。

**组件本身一行不用改**，包括换 tick 频率、换状态类型、关掉外推或重放。

---

## 9. 待你拍板的两点

1. **打包范围**：只在本仓库里做独立程序集（命名空间 `Starve.Netcode`），
   还是按"以后要搬到别的项目/发 NuGet"来做（中性命名空间，比如 `Netcode`）？
2. **泛型化程度**：按上面走**泛型 + 游戏侧模型**（可换状态类型，复杂度略高），
   还是先用 `Vector2` 特化版（简单快，但换状态类型要改组件）？
   —— 我倾向泛型，因为你明确说要复用；特化版省下的复杂度其实不多。
