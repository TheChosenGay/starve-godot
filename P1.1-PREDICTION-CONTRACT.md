# P1.1 客户端预测契约

客户端协议层实现：

- `Transport` 读取服务端 `1.1` 协议能力；
- `SessionInfo` 保存登录分配的 `input_epoch`；
- `InputSequenceTracker` 负责单一 epoch 内的 seq、ACK 和预测领先上限；
- `WorldService` 丢弃旧 epoch 与倒退 tick 的增量快照；
- `OwnMovementSim` 使用服务端 `effective_speed` 保持同公式预测，未确认输入超过 4 条时暂停领先；
- HUD 诊断显示 epoch、sent、ack、pending；
- `ProtocolSmoke --e2e` 验证 epoch 与 ACK 追平。

两端共用 `testdata/movement_golden.json`。`make check` 会先比较服务端源文件，再由
客户端测试执行同一组移动/碰撞向量。

完整协议定义见服务端 `docs/P1.1-移动预测协议契约.md`。
