# P0.1 客户端真实基线

> 状态：已固化（2026-08-19）  
> 服务端对应文档：`../starve/docs/P0.1-真实基线与架构决策.md`

## 责任边界

- `Starve.Protocol`：WebSocket、pomelo、protobuf、登录、请求关联和世界状态表；不依赖 Godot。
- `Starve.Core`：移动预测、等距投影、相机和位置平滑；不依赖 Godot，可单元测试。
- `GodotClient/Game`：输入、节点生命周期、渲染、HUD 和音画反馈；只通过 Core/Protocol 公开接口工作。
- 服务端是世界状态、碰撞和制作结果的唯一权威；客户端校验只用于即时反馈。

## 移动契约

- 输入发送整数方向 `dx/dy ∈ {-1,0,1}`；按住每 100ms 重发，松开立即发送 `(0,0)`。
- `OwnMovementSim` 保存整数锚点和 `[0,1)` 子格，逐轴执行与服务端相同的 `stepAxis`。
- 顺序固定为先 X 后 Y；对角速度除以 `sqrt(2)`；阻挡边界使用 `0.001/0.999`。
- 本地阻挡来自服务端实体快照中的 `Block + Position`，只有阻挡集合改变时才重建。
- 服务端快照用于校正；停止由服务端确认后落定。不得再实现第二套基于 `Floor(float)` 的跨格算法。
- 负方向跨格、负方向贴墙、对角归一化由 `Starve.Core.Tests` 覆盖。

## 相机与拾取契约

- `WorldPivot` 固定在视口中心并承担缩放、Q/E 水平旋转。
- `World` 只负责抵消相机跟随目标的等距投影，所以玩家旋转前后都保持在屏幕中心。
- 屏幕拾取必须通过 `World.ToLocal()` 再调用 `IsoMath.LocalToWorld()`；不得手写另一套角度补偿。
- 相机旋转是显示变换，不改变服务端坐标和 WASD 世界方向。

## 制作交互契约

- 工作站提示来自服务端快照里的真实 `Workstation + Position`，距离规则与服务端一致：整数锚点曼哈顿距离不超过 3。
- 配方按钮始终允许请求；材料和工作站不足显示“尝试制作”，服务端返回的稳定错误码翻译为玩家可读提示。
- HUD 只在背包、装备、配方、工作站或制作进度分段变化时重建，禁止每个世界快照销毁并重建控件。

## 验收

```bash
dotnet build GodotClient/GodotClient.csproj
dotnet test Starve.Core.Tests/Starve.Core.Tests.csproj
dotnet build ProtocolSmoke/ProtocolSmoke.csproj
```

手工验收：

1. 四个方向和对角线连续行走，不穿树、岩石或建筑；负方向贴墙不抖动。
2. 按 Q/E 时玩家保持屏幕中心，地图、实体和鼠标拾取同步旋转。
3. 靠近工作台制作成功；材料或工作站不足时点击后得到明确原因。
4. 制作面板打开时持续行走，帧率不因每 tick 重建按钮而明显下降。

## 变更规则

协议变化先改 protobuf，再更新两端；移动数学变化必须同批更新两端测试和基线文档。Godot 代码不得进入 Core，网络线程不得直接操作 Godot 节点。
