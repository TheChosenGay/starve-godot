<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="../Starve.Protocol/Starve.Protocol.csproj" />
    <!-- 组件模式 E2E 要用 GameRoot 那个适配器（ComponentOwnMovementSim），所以引 Core（它引 Netcode）。 -->
    <ProjectReference Include="../Starve.Core/Starve.Core.csproj" />
  </ItemGroup>
</Project>
