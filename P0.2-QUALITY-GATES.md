# P0.2 客户端质量门禁与移动观测

> 状态：已实现（2026-08-19）  
> 基线：从 `optimize/p01-client-experience-review` 创建的 `feature/p0.2-quality-gates`

## 统一门禁

```bash
make check
```

门禁会恢复依赖、构建 Godot 客户端和 ProtocolSmoke、运行 Core/协议测试，并比较客户端与服务端 protobuf 源文件及路由常量。

默认服务端目录为 `../starve`，可覆盖：

```bash
STARVE_SERVER_DIR=/path/to/starve make check
```

`.github/workflows/quality.yml` 执行同一入口，并从 `TheChosenGay/starve` 检出权威协议。服务端协议变更必须先合入，客户端再同步。

## 移动预测观测

`OwnMovementSim.Diagnostics` 提供纯逻辑指标：

- 最近一次服务端校正误差；
- 会话内最大校正误差；
- 软校正次数；
- 硬跳次数。

设置以下环境变量后，HUD 会显示这些指标：

```bash
STARVE_DEBUG_MOVEMENT=1
```

该数据只用于诊断，不参与预测算法和服务端判定。后续接指标后端时应通过适配器读取，不把监控 SDK 引入 `Starve.Core`。

## 当前验收

- Core 单元测试覆盖正负方向、阻挡边界、对角归一化、等距投影逆变换、停止死区和校正计数。
- pomelo 测试覆盖 request/response 编解码、varint 异常及分帧/粘包。
- 两端 protobuf 和路由常量完全一致。
- Godot 客户端和无引擎协议冒烟工具均可独立构建。

Godot 画面和输入体验仍需编辑器 F5 手工验收；当前 CI 不声明已经完成图形化端到端测试。
