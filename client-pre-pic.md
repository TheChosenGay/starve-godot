# 客户端预测 / 和解 组件设计（定稿）

> 目标：把"预测 → 服务端权威校验 → 重放 → 插值/外推"做成**一个可复用组件**。
> 引擎无关、协议无关、预测算法外置。同步调用（非异步）。

## 0. 分层

```
接入层（游戏/引擎相关，极薄）
  · 解析协议、区分本端/他端、喂数据、读结果、渲染
        │  NetSnapshot（中立结构）
        ▼
ClientSmoother（组件：三份缓存 + 链 + 版本 + 回放循环）  ← ServerClock
        │  base(state, version) + action
        ▼
预测模块（游戏侧实现：与**服务端同一套**移动/碰撞/ORCA，单步）
```

- 组件**不**解析协议、**不**知道地形/ORCA、**不**碰渲染与玩法。
- 预测模块是唯一耦合点；换游戏只换它。

## 1. 中立数据结构

```go
struct NetSnapshot {
    tick        int64     // 服务端世界 tick（权威状态对应的时刻）
    appliedSeq  uint64    // 服务端"已烘进这份位置"到第几号输入  ★必须有
    epoch       uint64    // 会话代号；变了就清空全部历史       ★必须有
    own         State     // 本端权威状态（连续位置：整格 + 子格）
    ownStopped  bool      // 服务端确认停止（Dir=0 且无路径）
}

struct State {            // 由游戏决定：我们的是 锚点/子格/速度
    ...
}

struct Action { dx, dy }  // 由游戏决定
```

## 2. ClientSmoother

```go
ClientSmoother {
    clock    *ServerClock
    model    Predictor        // 游戏侧注入

    netCache   SnapshotWindow // 权威快照，**按 tick 排序**
    localCache InputHistory   // 本端操作（seq / epoch / 生效 tick / action）
    otherCache map[uid]Remote // 别人的插值

    chain struct {
        state   State
        tick    int64
        version uint64       // ★单调递增，绝不从 tick 推导
    }

    OnCorrected func(Correction)   // 对外唯一出口：只给"最终值"
}

// —— 对外 ——
SetIntent(seq, epoch, action, nowMs)      // 意图变化时记一条
AdvanceTo(targetTick)                     // 每帧：正常推进
OnSnapshot(snap)                          // 收到权威数据
Remote(uid).Push(tick, state, vx, vy)     // 别人
Remote(uid).Sample(nowTick)               // 别人：插值/外推
State / StateVersion / StateTick          // 渲染与诊断
```

## 3. 唯一的推进路径（重放 = 同一条路）

```go
func (s *ClientSmoother) AdvanceTo(targetTick float64) {
    for s.chain.tick+1 <= int64(targetTick) {
        s.chain.tick++
        act := s.localCache.ActionAtTick(s.chain.tick)  // 没有这条就用当前输入
        next := s.chain.state
        used := s.model.Step(&next, act, TickMs/1000, s.chain.version)
        if used != s.chain.version {                    // 基于过期基准
            next = s.chain.state                        // 重算一次
            used = s.model.Step(&next, act, TickMs/1000, s.chain.version)
            if used != s.chain.version { reportVersionMismatch() }   // 仍不一致 → 显式报警
        }
        s.chain.state, s.chain.version = next, s.chain.version+1
    }
}
```

**正常推进和重放是同一句调用**，所以不存在"两套推进结果不一致"。

⚠️ 重放的**终点**有三条约束（少一条就会出问题，全都有测试守着）：

```
target = max(nowTick(含单程), 快照 tick, 链头已经预测到的 tick)
                │              │                └ 绝不能把时间拉回去：那会白扔掉已经算好的预测，
                │              │                  而且 predicted 推不动 ⇒ 误差在两个时刻之间比较
                │              └ 快照比"我们的现在"还新（延迟恢复/时钟落后）时，至少推到它那一刻
                └ 服务端"现在"；超过 MaxReplayTicks 时限幅（客户端就先落后着，别卡帧）
```

## 4. 本端更新逻辑

```
OnSnapshot(snap):
  0) epoch 变了 → 清空 netCache/localCache/otherCache，version++
  1) netCache.Insert(snap)        // 乱序容忍：按 tick 插；同 tick 后者胜；早于已应用 tick 丢弃
  2) localCache.Ack(snap.appliedSeq)
  3) before = chain.state                       // 当前显示的位置
     chain.state = snap.own                     // Rebase：起点是**权威状态**
     chain.tick  = snap.tick
     chain.version++                            // ★改链就 +1（tick 会倒退，version 不会）
     AdvanceTo(clock.TickAt(now))               // ★重放
     err = Distance(before, chain.state)         // 此时 err 才是"真失配"
  4) 分档（都要落到可测的度量上）：
       err <  Deadzone      → Kind=Deadzone   （只更新诊断）
       err <  TeleportDist  → Kind=Replayed   → OnCorrected（只发最终值）
       err >= TeleportDist  → Kind=Teleport   → OnCorrected
  5) 重放上限保护：TickAt(now) - snap.tick > MaxReplayTicks 时截断并标记
```

要点：
- **重放起点是权威状态**，不是"当前位置往后重放"；
- `err` 必须在 Rebase+重放**之后**测，否则会把网络领先量当成失配（老 bug）；
- `serverStopped` **没有特例** —— 停止只是"重放结果 == 权威"的普通情形；
- **重放只在组件内部跑**，不碰渲染/动画/音效/玩法。

## 4.5 本端的残差平滑：重放结果和"已经预测出去的显示位置"不一致怎么办

场景：用户已经预测着走了几格，这时重放回来，结果和当前显示位置差了 `err`。
**直接贴上去就是一次瞬移**（`err` 可能是半个格到一格多），尤其在用户正走动时更刺眼。

### 两个值，不是一个

```
State          = 重放结果（权威正确）—— 后续预测的基准，绝不带残差
RenderedState  = 残差平滑后的位置     —— 相机与角色节点读这个
```

**残差永不回流进模拟**，否则误差判据和下一次重放的基准都会被污染。

### 数学：残差是"位移"，按比例衰减

```
残差 residual = 校正那一刻的显示位置 − 当时的模拟位置      （一个位移，不随时间变）
每帧：RenderedState = State + k × residual                k: 1 → 0
```

**为什么不能在"模拟位置"与"旧显示位置"之间插值**：模拟一直在前进、起点固定，
两者交叉时插值会**停顿甚至倒退**（实测一帧零位移、下一帧双倍位移——正是要避免的"一顿"）。
只有"把残差按比例加到当前位置上"才是常数速率的衰减。

因此模型契约里需要状态空间代数：`Delta(from,to)` / `AddDelta(state,delta,k)`
（`Lerp` 由这两个默认实现；朝向这类非线性状态可以覆写）。

### 三条不变量（都有测试守着）

| 不变量 | 含义 |
| --- | --- |
| **接缝零跳变** | 校正那一刻，`RenderedState` 必须**等于**校正前屏幕上的位置。残差取"当前屏幕位置"而不是"旧的模拟位置"，所以连续收到校正也不会叠跳 |
| **必然收敛** | k 降到 0 时 `RenderedState` **精确**等于 `State`，不留残余 |
| **速率双边夹** | 每帧位移 ∈ `[模拟步长 − 上限, 模拟步长 + 上限]`。上界防瞬移，**下界防停顿**（下界那条是我做变异测试时才补上的） |

### 走动 vs 静止：用两个**速度**上限，不用两个时长

```
时长 = 残差实际大小 / 速度上限(下限兜底)   ⇒ 速度上限永远成立，不会被别的上限顶破
                                          （按 err 反推会被"没消化完的旧偏移"顶破，实测 5.33 > 5）
走动时上限 5 单位/秒  —— 能混进走路里，看起来只像"走得快了一点"
静止时上限 12 单位/秒 —— 站着不动时屏幕上的唯一运动就是这段平移，最显眼，要收得快
```

### 分档（在 Rebase + 重放**之后**测 `err`）

| err | 处理 |
| --- | --- |
| `< 死区`（0.03） | 直接对齐。跳这一点点看不见；平滑它反而会因反复收敛产生永久微抖 |
| 死区 ~ 直接贴阈值（1.5） | **平滑**（上面那套） |
| `≥ 1.5` | **直接贴**。这是"被墙挡住/被顶开/传送"的量级，掩饰反而假 |

⚠️ 判"直接贴"只看**本次的新失配 `err`**，**不看**屏幕上累积的偏移：
累积那部分正是平滑要消化的东西。拿它判定会把"延迟尖峰恢复后屏幕累积落后 2 格、
而本次只差 0.38"误判成传送（实测一次 2 格可见突跳）。

### 两个连带结论

- **高度按 `RenderedState` 采样** ⇒ 横向连续了，上下就连续了（"上下卡"就是横向单帧跳带出来的）；
- **相机必须跟 `RenderedState`**（不是 `State`），否则校正会被"相机滞后 + 回补"再放大一次；
  动画速度也应该来自**渲染位移**（`LocomotionPresentation` 本来就是这样算的），
  于是残差被吸收成一小段速度变化，脚也不会滑。

## 5. 他端（别人的实体插值）

```go
Remote {
    samples []{ tick, state, vx, vy }   // 按 tick 排
}

func (r *Remote) Sample(nowTick) State {
    playout := nowTick - InterpDelayTicks          // ★渲染在过去
    a, b := 找到 playout 两侧的样本
    if 落在两点之间        → Lerp(a,b,t)            // Interpolated
    else if playout > 最新  {
        beyond := playout - 最新.tick
        if 允许外推 && beyond <= MaxExtrapTicks && 有速度
                           → 最新 + 速度×beyond      // Extrapolated
        else               → 最新                   // Frozen：到顶冻结，绝不倒退
    }
    if nowTick - 最新.tick > StallTicks → Starved    // 断线，交由接入层提示/冻结
}
```

- **别人不预测**：只用服务端真实数据，代价是看到"过去"。这是结构性取舍。
- `InterpDelayTicks` 是"拿延迟换平滑"的旋钮；**建议 2~3**（20Hz 下 100~150ms），
  给抖动留余量（Fiedler 的经验：3 倍发包间隔，为的是连丢两个包仍有插值目标）。
- 外推只用在"播放时钟跑过最新样本"，**有界**、到顶**冻结**、服务端确认停止不外推。

## 6. 预测模块契约（关键前提）

```go
interface Predictor {
    // 从 base 推进一步；**必须原样回传 baseVersion**。
    // 若实现里存了任何跨步状态而基准已过期，要回传"它所依据的旧版本"，组件会拒绝并重算。
    Step(base *State, action Action, dtSeconds float64, baseVersion uint64) (usedVersion uint64)
    Distance(a, b State) float64
    Lerp(a, b State, t float64) State
    TryGetVelocity(state State) (vx, vy float64, ok bool)
}
```

### 契约：预测模块必须**对链无状态**

组件把"玩家移动推进"和"smoother 内部重放"都交给**同一个模型实例**，所以模型不能有任何
属于自己的、跨步演化的状态。精确三段：

1. **链状态全部在 `State` 里。**
   模型不得持有任何跨步演化、且会影响下一次 `Step` 结果的字段：
   自身速度（ORCA 的 self）、路径游标、调用计数、上一次的输入、第一次见到的基准……
   —— 全部必须放进 `State`。否则重放到一半被放弃时模型内部留在中间状态，不可复现。
2. **世界视图是允许的输入，但不得被演化。**
   地形 / 静态障碍 / 动态邻居 / 速度档位这类"由调用方整体替换的只读输入"可以持有，
   但不能在 `Step` 内部改动、也不能随调用次数累积。
   代价要认下来：`Step` 实际是 `(state, action, dt, 当时的worldView)` 的函数，
   世界视图变了，同样的 `(state, action)` 也会给出不同结果（重放用的是"现在"的邻居，
   不是"当时"的）—— 这一部分偏差由和解死区承担。
3. **不依赖调用顺序或次数**，并且 `Step` 必须原样回传 `baseVersion`。

还有一条形式约束：
4. **只做单步**（"给定基准 + 操作 → 下一步"）。链式推进由组件驱动；不要一次算多步。

> ⚠️ 第 3 条的版本回传是**诊断**不是**修复**：组件在版本对不上时会重算一次，
> 而这个重算只对"无状态模型"成立 —— 如果模型真有内部状态，重算只会让它更错。

### 契约是**可执行**的，不是注释

`Starve.Netcode/ModelContract.cs` 提供校验器，任何 `INetModel` 实现都应在自己的测试里跑：

```csharp
ModelContract.AssertContract(
    sharedModel,                      // 组件实际使用的实例
    () => new MyPredictor(worldView), // 工厂：期望值要在**干净实例**上算
    baseA, baseB, recordedActions, dtSeconds);
```

四项检查：幂等（同入参两次调用结果一致）、版本回传、**两条链交替穿过同一实例**。
最后一项是关键——幂等检查抓不到"把第一次见到的基准当世界原点"这类实例级泄漏，
而工厂是必需的（否则共享实例上第一条链会把原点留下，看着反而"一致"）。
组件测试里已用四类违规模型验证过：依赖调用计数、缓存首个基准、不回传版本 → 全部被抓。

> 基准由组件**传入**（不是预测模块反向去组件里拉）：等价于"有缓存就用链头、
> 没有就用最后权威位置"，但方向是推不是拉 —— 避免循环依赖与重入。
> 引导阶段（首次加入/传送/复活）链由第一份权威快照建立，之后**永远从链头往前**，
> 绝不每帧回到权威位置（那是"拿现在比过去"的老 bug）。

## 7. 网络异常分两种，处理完全不同

| 状态 | 判据 | 处理 |
| --- | --- | --- |
| 抖动 / 迟到 | 样本间隔 > 正常值（几十 ms） | **有界外推**，到顶**冻结**。绝不后退 |
| 断线 | 超过 `StallMs`（≥1s）无包 | 冻结 + 提示，等重连后**一次性对齐**（此时才允许跳） |

**不做**"超时回退到最后一个权威位置"——权威位置永远在过去，那会变成高延迟下周期性倒退。

## 8. 时间基准：ServerClock

组件内唯一读时间的地方，其余 API 全部收 `nowMs`/`tick`（便于单测用合成时间）。

```go
ServerClock {
    OnSnapshot(serverTick, nowMs, epoch, maxStepMs)  // 观测偏移 = now - tick*TickMs；
                                    // 本次最多移动 maxStepMs（不跳）；偏差 > ResyncMs 才硬同步
    TickAt(nowMs) float64
    LeadTicks(nowMs, snapshotTick) float64
}
```

用途：插值播放时钟 + 重放终点。治的是"每个样本把插值时钟重锚一次"造成的相位误差。

⚠️ `maxStepMs` 必须按**经过的墙钟时间**给预算（组件里 = `ClockMaxStepMs × 距上次喂时钟的 tick 数`），
不能"每份快照都吃满上限"：延迟恢复时一次帧里会补来一整个突发（实测 22 份），逐份 10ms
就是 220ms —— 时间轴一帧突进 4.4 tick，屏幕跟不上 ⇒ 一次可见突跳。

## 9. 服务端要补的三个字段

1. **`VelX/VelY` = 已被格子层接受的位移 / dt**（现在贴岸时谎报满速 → 外推朝墙里走）；
2. **快照补 `last_applied_seq`**（现在只有 `last_accepted_seq`；"收到"≠"已烘进这份位置"）；
3. **`last_applied_seq` 还要带"它是在哪一 tick 生效的"**。这是目前**唯一还没解决**的质量问题：
   输入的"生效 tick"是在**发送时刻**用当时的延迟估计冻结下来的，而单程延迟在抖动时无法在发送时刻预知。
   实测（200±80ms 方波抖动 + 每 400ms 换向）：意图时间线最多差 **2.9 tick**，
   方向反转把位置差放大成 2×2.9×0.5 ≈ **1 格**的平滑和解（好消息：全落在平滑档，0 次可见突跳）。
   有了这个字段，客户端就能**精确锚定**而不是估计。

## 10. 明确不做

- 延迟补偿（lag compensation）；
- 传输层（UDP/TCP/WebSocket 都行）；
- 输入采样（键鼠 → Action）；
- 渲染/动画/音效/相机；
- 玩法的权威判定（命中、伤害）。

**已知代价**：别人被渲染在过去 ⇒ 瞄的是他 `InterpDelayTicks` 前的位置。
所以战斗表现要收：本地只预测自己的挥击动画/音效，命中反馈等 `Impact` 事件回来再播。

## 11. 验收

- 组件自带测试，用**假模型**（一维匀速 + 可选墙）覆盖：时钟校正不跳变、
  输入按 seq 裁剪、快照按 tick 排序/去重、推进不重复消费时间、
  **重放确定性**（录制意图流 → 重放结果 == 权威）、版本过期被拒绝并重算、
  死区/传送分档、插值连续、外推有界且到顶冻结不倒退。
- 接入层用 `MoveTrace` 判据：`driftSpeed≈0`、`decision=NoError`、`dRendY` 无尖峰。

## 12. 对抗测试（延迟重放 / 延迟回退 / 微小差距）发现并修掉的问题

测试分三层，都**不启动 Godot**：

| 层 | 文件 | 覆盖 |
| --- | --- | --- |
| 组件单测（假模型 + 精确时钟，可手算/对拍） | `Starve.Netcode.Tests/DelayedReplayRollbackTests.cs`（37 条） | 延迟重放的逐位复现、小数 tick、限幅边界、回退接缝、同 tick 重传、亚死区噪声、指标出口 |
| 组件边界 | `Starve.Netcode.Tests/ReconciliationEdgeCaseTests.cs` | 分档边界、乱序、断流、突发、会话切换 |
| 整链路（真实 `OwnMovePredictor`） | `Starve.Core.Tests/NetcodeAdversarialLinkTests.cs` | 下行延迟尖峰、抖动回退、亚死区漂移、频繁换向、压力 |

**修掉的问题**（每条都有测试守着，且都是"单看某一帧好像对"的那种）：

1. **重放终点会落在链头后面**：一次超出上限的延迟尖峰（25 tick）里，客户端已经按帧预测到更前面，
   旧逻辑把终点截回"快照 + 上限" ⇒ `predicted` 推不动 ⇒ 误差在两个**不同时刻**之间比较 ⇒
   位置本来完全一致却假报 **2.5 格**回退。现在终点 = `max(限幅终点, 已预测到的位置, 快照 tick)`。
2. **时钟限速被突发放大**：一帧补来 22 份快照 ⇒ 逐份 10ms ⇒ 一帧 220ms ⇒ 时间轴突进 4.4 tick ⇒
   屏幕残差超上限 ⇒ 可见突跳。现在按"距上次喂时钟过了多久"给预算。
3. **"直接贴"判据用错了量**：旧逻辑在**累积残差** > `CorrectionSnapAbove` 时把平滑改判成直接贴。
   延迟尖峰恢复时屏幕累积落后 2 格、而本次新失配只有 0.38 ⇒ 一次 2 格突跳。
   现在只按**本次新失配 `err`** 判定；累积那部分正是平滑要消化的东西。
4. **过渡时长按 `err` 反推**：接缝零跳变意味着残差 = 新失配 + 没消化完的旧偏移，可以比 `err` 大 ⇒
   每 tick 修正量突破速度上限（实测 5.33 > 5 格/秒）。现在按**残差实际大小**反推。
5. **确认输入被裁光**：`Acknowledge` 必须 `keepLast = MaxReplayTicks + 4`，
   否则重放窗口用到的意图查不到、整段回落到"最新意图"（误差随延迟线性增长）。

**效果**（`NetcodeLinkSimulationTests`，"直接贴"占比）：

| 场景 | 修复前 | 修复后 |
| --- | --- | --- |
| 0 / 50 / 100ms | 0.00 / 0.17 / 0.33% | **0.00%** |
| 200ms + 20ms 抖动 | 0.50% | **0.00%** |
| 300ms + 50ms 抖动 | 1.33% | **0.00%** |
| 下行尖峰 100→1200→100ms | 恢复期 1 次突跳 | **0 次** |
| 传送 12 格 | 1 次 | 1 次（设计行为，不变） |
| 恒定意图 0/100/300ms | 接受 100%、误差 0 | 不变 |

**已知、且已量化的两处**（不装作没问题）：

1. **开局爬升期**：`_inputDelayTicks = 2 × 单程估计` 从 0 限速爬升（±0.5 tick/快照，故意不做时间轴突跳），
   所以头 1~4 秒内换向会带最多 **10~12 tick** 的时间线误差（约 4 格）。已用"爬升期不计入稳态判定"的
   测试把它和稳态质量分开看。**可选改进**：生效延迟本身可以立刻采纳首测值（它不影响预测目标，
   不像单程估计那样会造成时间轴突跳）。
2. **抖动下的换向**：见 §9 第 3 条 —— 稳态误差峰值 1.0 格（意图时间线最多差 2.9 tick），
   全部落在平滑档、**0 次可见突跳**；根治要服务端回传"最后一条输入在哪一 tick 生效"。

## 13. 还没决定的取舍：按 tick 对齐 vs 按输入序号对齐

**现状（按 tick 对齐）**：输入的"生效 tick" = 按下时的时钟 tick + 往返估计；
链条头 = 时钟 tick + 单程 ⇒ 生效点比链头**晚一个单程**：

```
按下之后，自己的预测要等 1 个单程才开始转向
（实测：单程 6.45 tick → 345ms；见 Starve.Netcode.Tests/InputResponseLatencyTests）
```

好处是链条与服务端**逐 tick 对齐** ⇒ 转弯几乎不产生和解（恒定意图误差恒为 0）。
代价就是上面那条**输入延迟** —— 之前所有测试都在量"位置误差"，量不出它，所以一直没被发现。

**另一条路（按输入序号对齐，业界标准做法）**：

1. **立刻**用最新输入预测（生效 tick = 链头）⇒ 零输入延迟；
2. 存一小段"**我自己的状态历史**"，按输入序号索引（几十 tick 的环即可）；
3. 快照带回 `last_applied_seq`（§9 第 2 条）⇒ 拿"序号 N 之后**我自己**的状态"对比
   "服务端序号 N 之后的状态" —— 比的是**同一个输入序列之后**的两个状态，
   所以**根本不需要知道它在服务端是哪一 tick 生效**，也就没有可猜的东西；
4. 只有两者真的不一致才 rebase：从权威状态 + 未确认输入重放。

代价：加一个状态历史环、把和解锚点从 tick 换成 seq，`MaxReplayTicks` 的限幅语义要跟着改。
收益：消掉输入延迟、消掉 §9 第 3 条的意图时间线误差、消掉 §12 里"抖动下换向 ~1 格"那一项。

**判定口径**：两条都要看 —— `InputResponseLatencyTests` 量输入延迟，
`NetcodeLinkSimulationTests` / `NetcodeAdversarialLinkTests` 量位置误差。
只看误差会把输入延迟藏起来（这次就是这样）。

### 9.1 服务端侧的第三个坑（2026-02 查证，代码位置见下）

"一个 tick 只应用一条移动操作" —— 同 tick 的多条操作会被**折叠**，不是叠加：

- `internal/game/systems/control_system.go`：`ControlSystem.Update` 对每个 Actor 取
  `ArrivalID` 最大的那条意图，其余标 `Superseded`（注意：是**到达顺序**，不是 seq）。
- `internal/game/world/world_actor.go:712 applyCommands`：`cmds.Handle(c)` 一成功就
  `ack.Seq = c.Seq` —— 而仲裁发生在其后的 ControlSystem 里。

⇒ 由此两个真实缺陷：

1. **`last_accepted_seq` 的真实语义是"已入队"，不是"已生效"**：被同 tick 覆盖掉的那条也会
   把 ACK 推进。客户端据此裁历史、并认定"≤N 都烘进权威位置"，在被丢弃的那条上就是错的
   （预测里那一转永远无法被重放复现 ⇒ 持续偏差，直到下一次输入）。
2. **`Superseded` 不下发**（只在内部计算），所以客户端**无法知道**自己哪条操作被丢了。
   同一 tick 内乱序（seq 51 先到、50 后到）时，赢的是**更旧**的 50 —— 会用旧方向覆盖新方向。

要做的（比"回执生效 tick"更靠前）：

- ACK 改成"**已生效**到哪个 seq"（在 ControlSystem 仲裁之后记，而不是 Handle 成功时记）；
- 回执**被丢弃/被覆盖的 seq**（`Superseded` 已经算好了，下发即可）；
- 客户端把被丢弃的操作从**预测的输入流**里剔除（否则预测永远追不上权威）；
- 同 tick 多条按 **seq 最大**者胜（而不是到达顺序），这样客户端能自己按同样规则折叠，
  "只留最后一条"变成可预测的。

## 14. 客户端输入采样的口径（2026-02 修正）

**长按不是"发一条、之后靠服务端沿用同一意图"** —— 那样服务端就没有密集的比对点，
每条的"时长"也不统一。正确口径是：

```
客户端：按**固定间隔**采样当前意图（本作 = 每个 tick 一条）→ 写进本地缓存（操作环）
        每条操作 = 一个 tick 的意图，序号是自增 uint64（与 tick 无关）
上行：  每个 tick 上传**未确认的操作**（冗余；被丢包也不丢输入）
        —— 包体可以用 RLE 压掉连续相同的意图
服务端：按 seq **去重 + 排序**（接受乱序补齐）进每个玩家一条队列（移动/攻击/取消共用）
        每 tick 按序消费；积压时追步（K 条 Move 配 K 步；Action 不占步）
和解：  环里"应用完第 N 条之后我自己的状态" vs 快照里"第 N 条之后的状态"
```

为什么长按也必须持续采样：

1. **比对点密度**：不和采样的话，一条"按住 ←"可能几分钟没有新操作 ⇒ 撞墙/被顶开这类分歧
   要等到下次改变方向才被发现；
2. **时长统一**：每条操作恒定占一个 tick ⇒ 抖动只影响"什么时候应用"，不影响"持续了几个 tick"
   ⇒ 同序号状态严格可比（不需要往协议里塞任何时间戳）。

> 📌 **本套机制的权威文档**：客户端 [P1.7 序号锚定网络层](P1.7-序号锚定-命令流-追赶-渲染.md)、
> 服务端 [P1.3 输入流：序号消费与多步追赶](../starve/docs/P1.3-输入流-序号消费与多步追赶.md)。
> 下面 §15/§16 是这一路的过程记录与实测数据。

## 15. 端到端门禁（真服务端 + 无头客户端，不启动 Godot）

单元/仿真测试用的是**模拟服务端**；为了验证"真服务端 + 真协议 + 真组件"的配合，仓库里有两条门禁：

```bash
# ① 传统链路门禁（协议能力/epoch/tick/ACK/移动契约）
make e2e                       # 需要先起 gate：GATE_WS_ADDR=127.0.0.1:18099 /tmp/starve-gate &

# ② 序号锚定端到端扫描（组件模式，上行加受损：延迟/抖动/丢包/乱序）
make netcode-sweep             # 自己起临时 gate（全新存档），跑完自动停
```

`ProtocolSmoke --netcode` 用的就是 GameRoot 那个适配器（`ComponentOwnMovementSim`），受损上行是
**应用层**的（整条操作不发 / 延迟到后面才发）—— 比 WS(TCP) 的真实行为更狠，用来压测
"冗余窗口 + 缺口宽限 + 和解"这条链。

诊断开关（排查时才开）：

| 开关 | 作用 |
| --- | --- |
| `STARVE_NETCODE_TRACE=1` | 每次有实际内容的和解打一行：锚点是否命中、误差构成、服务端/客户端位置、意图、障碍/邻居条数 |
| `STARVE_NETCODE_E2E_DEBUG=1` | 每 100 帧打印一次内部状态 |
| `STARVE_SMOKE_NO_BLOCKERS=1` | 关掉客户端障碍/邻居喂料（做"是不是模型差异"的对照实验） |
| `GATE_LOG_LEVEL=debug` | 服务端每次采样把 `cmd_backlog/cmd_max_backlog/catchup_steps/dropped_ops/dropped_ticks/desyncs` 打出来 |

### 判据（哪些是硬不变量，哪些是模型容差）

**与地形/障碍模型无关的硬判据**（这些挂了就是 netcode 真的坏了）：

- **序号锚定命中**：`ack` 指向的操作必须一直在客户端回看环里（实测 8 档全部 `400/400`）；
- **操作流被吃干净**：`sent − ack ≤ 3`，且每个操作都被确认（自愈）；
- **无校正风暴**：平滑校正次数 ≤ 2 × 快照数 + 20；
- **末尾收敛**：结束时的锚定误差 ≤ 1.5；
- **输入延迟 ≈ 1 tick**（实测 40~43ms）。

**模型容差项**（放宽带，只用来抓"发散"）：直接贴次数 ≤ 15、干净链路误差峰值 ≤ 3.0。
原因：无头客户端拿不到服务端那份完整世界（全局碰撞索引/AOI 裁剪后的障碍子集与顺序、地形高度、AI），
服务端每 tick 的位移是「速度×坡度 + 形状滑动 + ORCA」，贴障碍走时必然分叉出 1~2 格。

### 修过的真问题（都在这条链路上暴露出来）

1. **服务端"没操作也白走一步"**（`control_system.go`）：操作驱动的玩家在没消费到操作的 tick 里，
   MoveSystem 仍按保留意图跑 round 0 白走一步，而 ACK 不涨 ⇒ "第 S 条操作之后的状态"在两边
   不是同一个状态（恒定偏 0.5 格 = 一个 tick，转向处翻倍）。修法：这类 Actor 每 tick 在 `Steps`
   里占一个**空条目**，并给它把速度归零。回归测试 `op_driven_movement_test.go`（已做突变验证）。
2. **客户端冗余窗口不重发"最旧未确认那条"**（`ClientSmoother.CollectUnackedOps`）：窗口起点取了
   "最近 max 条"的下界，未确认数超过 max 时 `ack+1`（服务端唯一可能在等的那条）永远不再发
   ⇒ 丢一条操作就**永久少走一条位移**。修法：头部固定带 `ack+1` + 其余名额给最新若干条；
   冗余条数提到 4（`NetcodeConfig.RedundantOps`）。回归测试
   `RedundantWindowAlwaysCarriesTheOldestUnackedOp`。
3. **位置与 ack 跨消息配对**（`WorldService.ReadAtomic` / GameRoot）：推送在**网络线程**上直接改
   世界并顺带推 ack，主线程分两次读就会配出「位置来自消息 m、ack 来自 m+1」，锚定比较整体偏一个
   tick（每次快照都误判成需要校正）。修法：用 `Revision` 做 seqlock 的原子读。
4. **ORCA 邻居永远是空的**（`GameRoot.SyncOrcaNeighbors`）：判据写成 `view.Has("Dynamic")`，而服务端
   早已把 `Static/Dynamic` 两个 tag 删掉、换成"有 Moveable = CanSelfMove" ⇒ 本地预测从不做动态
   避让。修法：判据改成"有 Moveable"。

### 实测（8 个受损档，`make netcode-sweep`，4 轮连续全绿）

| 上行受损 | sent/ack | 锚定 | 误差峰值 | 平滑/直接贴 | 输入延迟 |
| --- | --- | --- | --- | --- | --- |
| 干净 | 144/144 | 400/400 | 1.42 | 238 / 0~8 | 43ms |
| 50 / 100ms | 144/144、143/143 | 400/400 | 0.76~1.83 | 212~331 / 0~8 | 40~43ms |
| 200 / 300ms + 抖动 | 1573/1573 | 400/400 | 0.51 | 314~327 / 0 | 41~43ms |
| 100ms + 30% 丢包 | 1330 提交/392 丢 | 400/400 | 0.75 | 304 / 0 | 43ms |
| 200+40ms 抖动+20% 丢+20% 乱序 | 1572 提交/308 丢/255 乱序 | 400/400 | 1.12 | 348 / 0 | 42ms |
| 300+50ms+40% 丢+30% 乱序（极端） | 1572 提交/644 丢/251 乱序 | 400/400 | 0.88 | 387 / 0 | 42ms |

结论：**操作流逐条被消费/确认、锚定从不脱靶、延迟恒为 1 tick、校正不成风暴**。
干净链路偶发的 3~8 次"直接贴"来自上面那条**模型可比性**（关掉障碍喂料、加大出生点平地，
分布都不变），不是接线或算法问题；要彻底判定它，只能用真客户端跑一次并看
`starve-netcode-<stamp>.jsonl` 的校正统计（无头测试台做不到这一点）。

## 16. 自己角色的渲染：tick 之间怎么补（走路"一卡一卡"的落点）

模拟只走**整 tick**（20Hz，每 50ms 一步），渲染要按帧（60fps）画。一帧里"走完最后一个整 tick 之后
剩下的那部分时间"就是**帧余量**（0~1 个 tick 的比例）。用它把整 tick 的位置补成"当前这一刻"的位置，
就是 tick 之间的插值。两种补法由 `NetcodeConfig.OwnRenderDelayTicks` 选：

| 值 | 补法 | 特点 |
| --- | --- | --- |
| **1.0（默认，插值）** | `lerp(S(n-1), S(n), 帧余量)` | 两个端点都是**已知**的 ⇒ 逐帧位移严格线性，不会"猜错再回抽"；代价是画出来比最新预测**晚约一个 tick** |
| 0.0（外推） | `S(n) + 帧余量 × 上一步位移` | 零额外延迟，但那是**对未来半个 tick 的猜测**：坡度/撞墙/转向/校正都会让猜测落空 ⇒ 下一个整 tick 落地时"抽回来" |
| 0.5 | 折中 | 只外推半个 tick |

手感 A/B（不用改代码）：`STARVE_OWN_RENDER_DELAY_TICKS=0 make run`。

**接缝（校正）必须零跳变**——这是逐帧追出来的第二个坑，三种做法实测：
1. rebase 时把插值端点**清零**：那一帧渲染贴到最新状态（比插值点靠前近一个 tick），下一帧再回来
   ⇒ 校正频繁时（走动中约 43% 的帧带校正）`offsetDist` 在 0.03↔0.34 之间跳、逐帧位移 ±0.2 来回；
2. **保留旧端点**：Δ 变成"校正量 + 一步"，插值偏移被放大（单测实测渲染 0.75 → 0.00）；
3. **把端点跟着校正量一起平移**（现行）+ 残差从**基座**（不含插值偏移）播种 ⇒ Δ 不变、接缝零跳变，
   校正量全部交给残差平滑消化。

逐帧追踪（诊断用，默认关）：

| 开关 | 作用 |
| --- | --- |
| `STARVE_LOCO_TRACE=1` | 每帧落盘：本帧位移 → moving 判定 → 动画片段/速度，以及渲染时基 `tickTarget`、插值偏移 `offsetDist`、残差权重 `blendWeight`（默认 `/tmp/starve-loco-trace.csv`） |
| `STARVE_MOVE_TRACE=1` | 已有的三条线（本地预测 / 服务端权威 / 渲染位置）CSV |
| `STARVE_DEMO_MOVE="1,0"` + `STARVE_DEMO_PATROL_MS=4000` | 无人值守自动行走；带 PATROL 就每 4 秒反向（原地往返，不会被顶到地图边界/障碍上） |

实测（真客户端，走路帧）：

| 指标 | 外推（旧） | 插值 + 接缝修复 |
| --- | --- | --- |
| 相邻走路帧速度差 p50 / p95 | 5.29 / 15.23 格/秒 | **0.02~0.04 / 0.18~0.35** |
| 插值偏移 `offsetDist` | 0.03↔0.34 跳 | 0.13（p50）~0.43（p95），恒在一个 tick 内 |
| "按着键却被判停"帧 | 16% | 6%（都在转向/贴障碍那几帧） |

⚠️ 动画不是元凶：它的"走/停"和播放速度是**拿这一帧渲染位移算的**
（`LocomotionPresentation.FromDisplacement`，阈值 0.8 格/秒），所以位移一抖它就忽快忽慢；
被切成 idle 再切回来时 `Play("walk")` 会从第 0 帧重播。位置顺了，动画自然顺。
