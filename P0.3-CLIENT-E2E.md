# P0.3 客户端诊断与协议 E2E

> 状态：已实现（2026-08-20）

## 诊断边界

`Starve.Core/OwnMovementSim.cs` 继续只维护纯逻辑状态，并通过
`MovementDiagnostics` 提供稳定拉取接口。Core 不引用 OpenTelemetry、
Prometheus 或 Godot。

设置 `STARVE_DEBUG_MOVEMENT=1` 后，Godot 适配器每秒拉取一次快照：

- HUD 使用最近一次采样值，相同文本不重复写入控件；
- 仅当诊断值变化时写一条 `MOVEMENT_DIAGNOSTICS` 本地日志；
- 日志只包含误差和校正计数，不包含 UID；
- 客户端不监听 Prometheus 端口，也不主动上报遥测。

## 自动协议 E2E

服务端已运行时执行：

```bash
STARVE_GATE_URL=ws://127.0.0.1:8081/ws make e2e
```

`ProtocolSmoke --e2e` 有全局超时，任一断言失败返回非零，并验证：

1. WebSocket/Pomelo 握手和 JWT 登录；
2. 全量快照包含登录玩家及基础组件；
3. 后续增量推进世界 tick，且合并不会冲掉玩家组件；
4. `Moveable` 的方向、速度、`sub ∈ [0,1)`、实际位移和停止确认。

手工入口仍可使用：

```bash
dotnet run --project ProtocolSmoke -- 42 --diag
dotnet run --project ProtocolSmoke -- 42 --movetest
```

可通过 `--url`、`--uid` 和 `--timeout-seconds` 覆盖连接参数。

## CI 临时 gate

质量工作流先运行不依赖在线服务的 `make check`，再：

1. 在临时目录检出服务端；
2. 使用独立端口和临时存档启动 gate；
3. 等待 TCP 就绪后运行 `make e2e`；
4. 无论成功失败均发送 `SIGTERM`、等待进程退出并输出 gate 日志。

CI 不重写服务端源码。默认检出服务端 `main`，要求它已经具备 P0.2
可移植鉴权；跨仓库联调时可通过仓库变量 `STARVE_SERVER_REF` 指向已推送的
服务端分支。依赖尚未合入或分支尚未推送时门禁失败，用于明确暴露合并顺序问题。
