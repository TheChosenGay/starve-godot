# P1.7 序号锚定网络层：命令流、和解、多步追赶与渲染

> 上游：`P1.1`（客户端预测契约）、`P1.5`（算法：预测/和解/重放/插值）、`P1.6`（组件边界）、
> 服务端 `docs/P1.1-移动预测协议契约.md`、服务端 `docs/P1.3-输入流-序号消费与多步追赶.md`。
>
> 本文是**落地后的最终语义**，以及这一路**踩过的坑**（现象 → 根因 → 修法 → 证据/回归）。
> 想直接看代码：`Starve.Netcode/ClientSmoother.cs`、`Starve.Core/ComponentOwnMovementSim.cs`、
> `GodotClient/Game/GameRoot.cs`、服务端 `internal/game/systems/control_system.go`。

---

## 0. 一页速览

| 规则 | 说明 |
| --- | --- |
| **一条操作 = 一个 tick** | 客户端每个整 tick 采一条带序号的"意图"，长按就每 tick 重复一条（意图没变也发）。 |
| **seq 是纯自增 uint64** | 只用于去重/排序/ACK，**与 tick 无关**；move/attack/craft/cancel **共用一条流**。 |
| **上行 = 头部 `ack+1` + 最新 N−1 条** | `RedundantOps=4`。头部那条是**服务端唯一可能在等的**，不重发它 = 把缺口永久化。 |
| **服务端连续消费** | 按 seq 插入排序（乱序容忍、去重），只从 `acked+1` 连续消费；缺口等 3 tick（150ms）后跳过并计 `Desyncs`。 |
| **K 条 move = K 步移动** | 每 tick 最多 `StepBudget=3` 步，每步用**那条操作自己的方向**（多步追赶）。 |
| **ACK = 已消费的 seq** | 在 `RunSystems`（含移动）**之后**提交 ⇒ "这条已经烘进本 tick 的自己状态"。 |
| **没操作就不许走** | 操作驱动的 Actor 在没消费到操作的 tick 里**一步都不走**（不许按保留意图白走），速度归零。AI/生物仍按 tick 走。 |
| **和解比"同序号状态"** | 客户端环里存"执行完第 N 条之后我自己的状态"，直接和服务端第 N 条之后的权威位置比 —— **不估在途时间**。 |
| **渲染只做插值** | 模拟走整 tick、渲染按帧；默认在"上一条 tick 状态 → 最新 tick 状态"之间插值（比最新预测晚约 1 tick）。 |
| **位置与 ack 必须同一条消息** | 推送在**网络线程**改世界 ⇒ 分两次读会跨消息配对，锚定整体偏一个 tick（见 `WorldService.ReadAtomic`）。 |

---

## 1. 命令怎么流（协议契约）

### 1.1 客户端采样与上行

```
每个整 tick（AdvanceTo 的整 tick 循环里）：
  _currentOpIndex = ++_opSeq            // 纯自增，不等于 tick 号
  Inputs.Record(seq, epoch, 意图)        // 意图 = 当前按键方向；没按键就是 (0,0)
  StepOnce()                            // 用这条操作的方向走一步（整 tick，dt=50ms）
  环里存 (seq, 执行完这条之后我自己的状态, 动作)

每帧（约 60fps）：
  batch = CollectUnackedOps(RedundantOps=4)
        = [ack+1] + [最新 3 条]           // 头部是缺口补丁，最新是保流不停 + 丢包覆盖
  SendMoveOps(batch)                     // 用**原 seq** 重发；服务端按 seq 去重
```

* **为什么每 tick 都要发**：服务端只认"操作流"，不认"最后一条决策"。长按 = 每 tick 一条同方向的操作流；短按只覆盖 1~2 个 tick。服务端因此能把"我按了多久、什么时候松手"逐条复现。
* **为什么离散操作共用一条 seq**：服务端按 seq **连续**消费，两条流各自编号就会互相插队/缺号 ⇒ 顺序错乱、缺口永远补不上。攻击/合成/取消用 `ReserveDiscreteOp` 从同一条流取号，但**不占移动步**（它们不产生位移，所以"执行完它之后的状态"就是当前状态，两边同序号仍然相等）。
* **冗余窗口的两个坑**（都踩过）：
  * 只发"最近 N 条" ⇒ 未确认数超过 N 时最旧那条永远不重发，而服务端只会卡在 `ack+1` ⇒ **丢一条操作 = 永久少走一格**（0.5 格 = 一个 tick 的位移）。
  * 只发"最旧那条 + 最新" ⇒ 高延迟下 ack 滞后很多，名额被早已确认的几条吃掉，**新操作发不出去**、流停住、预测跑飞（仿真里误差峰值 27）。
  * 最终形态：**头部 1 条固定 + 其余给最新 `N−1` 条**（`NetcodeConfig.RedundantOps = 4`）。

### 1.2 服务端消费（含多步追赶）

| 参数 | 值 | 含义 |
| --- | --- | --- |
| `StepBudgetDefault` | 3 | 单 Actor 单 tick 最多消费几条 **move**（= 最多走几步） |
| `MaxPendingDefault` | 8 | 单 Actor 队列上限；超出丢**最旧**并 `Dropped++` |
| `maxGapWaitTicks` | 3 | 缺口最多等几个 tick（150ms），等不到就跳过并 `Desyncs++` |
| `maxTickDebt` | 4 | 世界时钟落后墙钟超过这么多 tick 就丢 tick（`DroppedTicks++`） |
| `GATE_TICK_MS` | 50 | 服务端 tick 间隔（20Hz） |

* **插入**：命令到达即按 seq 插入该玩家队列（`insertBySeq`），已在队列里的 seq 直接忽略（`hasSeq` 去重）——因为客户端每帧都在重发未确认窗口。
* **消费**：从 `consumed+1` 开始**连续**消费；不连续就等（缺口），等到超过宽限才跳过。**连续**是"两边操作流逐条对应"的前提。
* **追赶**：一次 tick 可以消费 K 条（K ≤ `StepBudget`），**每条配一步移动积分**，每步用那条操作自己的方向。没有追赶，网络抖动（几条操作挤进同一个 tick）会让队列变成随机游走、玩家的操作被**永久**推迟。
* **ACK**：`commitConsumedInputs()` 在 `RunSystems`（含 MoveSystem）之后执行，把本 tick 真正消费掉的 seq 推到 `last_accepted_seq`。被**仲裁层**拒绝的操作（目标无效/忙碌）也推进 ACK —— 它确实被处理过了，否则客户端 pending 会无界增长。
* **操作驱动 vs AI/生物**：收到过带 seq 操作的 Actor 记进 `ControlQueue.OpDriven`，每 tick 在 `Steps` 里占一个条目（**没有操作时是空条目**）。MoveSystem 的 `participatesInRound` 对"有表"的实体只跑到自己步数用完为止，于是**不会替它跑保留意图的第 0 轮**。本 tick 无操作时还要把 `VelX/VelY` 归零，否则别的客户端会拿上一 tick 的速度继续外推。AI/生物没有 seq（不进 `OpDriven`），语义不变：按 tick 自己走。
* **epoch**：`BeginInputEpoch` 换世代 → `ResetActor`（清 `Pending/Steps/consumed/gapWait/OpDriven`），旧 epoch 的命令丢弃。断线重连因此不会把旧流和新流接在一起。
* **快照一致性**：`tick / input_epoch / last_accepted_seq` 与**位置**在**同一条消息**里，且位置 = "执行完 `last_accepted_seq` 条操作之后的状态"（这就是"序号锚定"成立的全部前提）。

### 1.3 时序（本机、20Hz、约 1 tick 输入延迟）

```
客户端 tick T      : 采样 op#N（方向 D）→ 本地走一步 → 入环
客户端帧           : 发 [ack+1 .. 最新]（冗余）
服务端 tick T+1    : 收到 op#N → 入队 → 本 tick 消费（≤3 条）→ 每条约走一步 → commitConsumedInputs
服务端 tick T+1 快照: 位置（已烘进 op#N）+ last_accepted_seq = N
客户端 tick T+2    : 收到快照 → 取环里 op#N 的状态比权威位置 → 死区/平滑/直接贴
```

实测输入延迟 ≈ **40~43ms（≈1 tick）**。

---

## 2. 客户端预测与和解

### 2.1 与服务端同一套数学（缺一不可）

| 项 | 服务端 | 客户端 | 喂料入口 |
| --- | --- | --- | --- |
| 坡度速度 | `SlopeFactorAt(map.HeightAt)` | `SlopeSpeed.Factor(_tilemap.LogicalHeightAt)` | `sim.HeightAt = _tilemap.LogicalHeightAt` |
| 地形卡边界 | `stepAxis(..., walkable)`；`walkable` = 越界/角地形为水 | `OwnMovePredictor.StepAxis(..., _walkable)`；`IsWalkable` 同判据 | 构造时注入 |
| 静态占位物 | 静态碰撞层（**不自己动**的实体） | `MovementSlide` 扫掠 + 沿切面滑动 | `SetBlockers(只有没有 Moveable 的实体)` |
| 动态避让 | ORCA（`CanSelfMove` = 有 Moveable） | ORCA（同判据） | `SetNeighbors(有 Moveable 的实体)` |
| 速度 | `effectiveSpeed = Speed × 效果修正`（**不含坡度**） | `SetSpeed(mv.EffectiveSpeed)`，坡度在步进里乘 | 快照 |
| 身体半径 | `Collide.Radius` | 同 | `SetBodyRadius` |
| 路径 | `mv.Path` 队首优先于输入方向 | 无本地意图时跟服务端 Path 队首 | 快照 |

⚠️ **只喂静态形状**是最容易踩的坑：本地 `MovementSlide` 把**胶囊**当硬障碍、且**不看 `Owner`**，把"会自己动"的实体（尤其是**玩家自己的胶囊**）喂进去，等于每步都在撞自己（实测本地 0.073 格 vs 服务端 0.5 格）。

### 2.2 和解（`ReconcileByOpIndex`）

1. 拿快照的 `AppliedSeq` 在环里找**同序号**的条目（`op.StateAfter`）。
2. `err = |op.StateAfter − 快照位置|` —— 两边都是"同一操作前缀之后"的状态，**不含网络领先量**。
3. 分档：`err < CorrectionDeadzone(0.03)` = 死区（不动）；`< CorrectionSnapAbove(1.5)` = 平滑（rebase + 用**我自己的操作流**重放到链头，残差交给渲染层按 `MaxCorrectionSpeed=5 格/秒` 收敛）；`≥ 1.5` = 直接贴（撞墙/传送，不掩饰）。
4. **找不到锚点**（首帧、延迟超出环长、换会话）：**只贴位置、绝不把时间轴推走** —— 否则活推进会一帧跨完一大段，把采样节拍整段跨过去（实测：链头 tick 涨 142，操作却采不出 0 条，客户端完全不动）。
5. 环容量 `OpHistoryCapacity=64`（3.2 秒回看）；每条移动操作记录**自己那一步实际走了多少格**（`StepDistance`），诊断用（不受 rebase 污染）。

### 2.3 必须原子读

推送在**网络线程**上直接改世界并顺带推 ACK（`Session.OnPacket → World.HandleMessage`）。主线程分两次读"位置"和"ack"就会配出「位置来自消息 m、ack 来自 m+1」，锚定比较整体偏一个 tick ⇒ 每份快照都误判成需要校正。修法：`WorldService.ReadAtomic(...)`（用 `Revision` 做 seqlock）。

---

## 3. 渲染：tick 之间怎么补

模拟走**整 tick**（20Hz），渲染按帧（60fps）。一帧里"走完最后一个整 tick 之后剩下的时间比例"就是**帧余量**。

| `OwnRenderDelayTicks` | 补法 | 特点 |
| --- | --- | --- |
| **1.0（默认）** | `lerp(S(n−1), S(n), 帧余量)` | 两个端点都是**已知**的 ⇒ 位移严格线性、不会猜错回抽；代价是比最新预测晚约 1 tick |
| 0.0 | `S(n) + 帧余量 × 上一步位移` | 零额外延迟，但对未来半个 tick 是**猜测**：坡度变化/撞墙/转向/校正都会让它落空 ⇒ 下一个整 tick 落地时"抽回来" |
| 0.5 | 折中 | 只外推半个 tick |

**接缝（校正）必须零跳变**。三种做法实测：端点清零（那一帧贴到最新状态，下一帧再回来 ⇒ 校正频繁时偏移在 0.03↔0.34 之间跳）、保留旧端点（Δ 变成"校正量 + 一步"，插值被放大 —— 单测实测渲染位置 0.75 → 0.00）、**跟着校正量平移端点 + 残差从基座 `_rendered` 播种**（现行，零跳变）。

**动画是受害者不是元凶**：`LocomotionPresentation.FromDisplacement` 用**这一帧的渲染位移**算"走/停"（阈值 0.8 格/秒）和播放速度，所以位置一抖，走路片段就忽快忽慢、被切成 idle 再切回来还会从第 0 帧重播。

---

## 4. 踩坑清单（现象 → 根因 → 修法 → 证据）

| # | 现象 | 根因 | 修法 | 证据/回归 |
| --- | --- | --- | --- | --- |
| 1 | 长时间走后整体发卡 | `PositionSmoother` 浮点虚拟时钟在大 tick 下精度丢失 | double 化虚拟时钟 | Phase 7 单测 |
| 2 | 走动时恒定偏约半格、每份快照都校正 | **服务端**：没消费到操作的 tick 仍按保留意图白走一步，而 ACK 不涨 ⇒ "第 N 条之后的状态"不存在 | `OpDriven` + 空 `Steps` 条目 + 速度归零 | `op_driven_movement_test.go`（已突变验证） |
| 3 | 丢一条操作后永久少走一格 | **客户端**冗余窗口只发"最近 N 条"，`ack+1` 永远不重发 | 头部固定带 `ack+1`；`RedundantOps=4` | `RedundantWindowAlwaysCarriesTheOldestUnackedOp` |
| 4 | 每份快照都像"需要校正" | 位置与 ack **跨消息配对**（网络线程改世界） | `WorldService.ReadAtomic` | 端上 err 从 1.5→1.2 量级、门禁从 flaky 变稳定 |
| 5 | 贴人/动物走被反复校正 | `SyncOrcaNeighbors` 判据用了**已删除**的 `Dynamic` ⇒ 邻居表恒空 | 判据 = "有 Moveable"（= 服务端 `CanSelfMove`） | 双方判据注释对齐 |
| 6 | 走路一卡一卡，本地步长被截断 | **本地预测撞自己**：`RebuildBlockers` 把含**自己胶囊**在内的所有 `Collide` 都喂进硬碰撞，而 `MovementSlide` 不看 `Owner` | 只喂**静态形状**（没有 `Moveable`） | A/B：全喂 29% 校正 → 只喂胶囊 47% → 只喂圆 6% → 修后 10~11%；err 中位 0.257 → 0.05 |
| 7 | 直线走也有 5% 帧**倒退** | 渲染拿帧余量**外推**（猜未来），猜错就抽回 | tick 间**插值**（`OwnRenderDelayTicks=1`） | 真客户端相邻帧速度差 5.29 → 0.02~0.04 格/秒 |
| 8 | 校正瞬间画面跳一下 | rebase 时插值端点处理错了（清零/保留都错） | `RebaseRenderEndpoints` 跟着校正量平移 + 残差从基座播种 | `CorrectionDoesNotJumpTheRenderedPosition`、`RenderedMotionIsUniformAtSixtyFps` |
| 9 | 客户端完全走不动 | **测试环境**：两个自动行走客户端挂在同一台 gate、同一个 uid 抢实体 | 追踪探针加自动退出（`STARVE_LOCO_TRACE_MS`）；自动测试只用独立 gate | 进程清理 + 隔离 gate 复测 |
| 10 | 端到端门禁随机红 | **测试台**：无头客户端跨消息配对 + 没喂世界模型；干净链路的 hard 阈值还包含模型可比性 | 原子读 + 按 GameRoot 喂料；门禁判据改成"与地形无关的强不变量" | 8 档扫描 4 轮连续全绿、锚定 400/400 |

> 经验：**验证前先核对"正在运行的二进制"**。曾出现"客户端一切正常但用户仍卡"，最后发现 gate 是 9 小时前 `go run` 编译的旧进程（不含当天修复）。

---

## 5. 诊断工具箱

| 开关 | 作用 |
| --- | --- |
| `STARVE_NETCODE_TRACE=1` | 无头协议客户端的逐帧追踪（锚点是否命中、误差构成、服务端/客户端位置） |
| `STARVE_LOCO_TRACE=1`（+ `_MS` 自动退出 / `_OUT`） | 真客户端逐帧"走路表现"：本帧位移 → moving 判定 → 动画片段/速度；另附渲染时基 `tickTarget`、插值偏移 `offsetDist`、残差权重 `blendWeight` |
| `STARVE_MOVE_TRACE=1`（+ `_MS` / `_N` / `_OUT`） | 三条线并排 CSV：本地预测 / 服务端权威 / 渲染位置（含 drift、srvStep*） |
| `STARVE_DEMO_MOVE="dx,dy"`（+ `STARVE_DEMO_PATROL_MS`） | 无人值守自动行走；带 PATROL 就每 N 毫秒反向（原地往返，不会被顶到边界） |
| `STARVE_OWN_RENDER_DELAY_TICKS` | 渲染 A/B：1=插值（默认）、0=外推、0.5=折中 |
| `STARVE_OWN_NO_BLOCKERS` / `NO_NEIGHBORS` / `ONLY_SHAPE=circle\|box\|capsule` | 喂料 A/B（判断残差来自哪类世界输入） |
| `GATE_LOG_LEVEL=debug` | 服务端采样 tick 打出 `cmd_backlog / cmd_max_backlog / catchup_steps / dropped_ops / dropped_ticks / desyncs` |
| `make netcode-sweep` | 真 gate + 无头客户端的 8 档受损端到端扫描 |

**常开的 netcode jsonl**（`<用户数据目录>/perf/starve-netcode-<stamp>.jsonl`，每次和解一行）：

| 字段 | 读法 |
| --- | --- |
| `k` = `Bootstrap/Deadzone/Blended/Snapped/Stale` | 这次和解做了什么决定 |
| `seq` / `snapTick` / `stateTick` | 锚定的操作序号 / 权威 tick / 我的链头 tick |
| `err` | **同序号**的失配（不是网络领先量） |
| `blendTicks` | 残差要几个 tick 收完；**持续变大 = 残差在叠加**（发卡信号） |
| `cmv` / `smv` / `dops` / `csteps` | 同一 ack 区间"我走了多少 / 服务端走了多少 / 跨几条操作 / 我实际走了几步" |
| `step` / `slope` / `eff` | 我最近一步的位移 / 客户端坡度因子 / 客户端手上的服务端有效速度 |

判读顺序：**锚定命中（必须≈100%）→ `sent−ack` 有界（流被吃干净）→ `err`/`blendTicks` → `csteps` vs `dops`（步数是否一致）→ `cmv` vs `smv`（同距离还是不同向）→ `slope`/`eff`（坡度模型、速度是否过期）**。

---

## 6. 验证矩阵

| 层 | 内容 | 现状 |
| --- | --- | --- |
| Go 不变量 | K 条 move = K 步；没操作不许走；连续消费/缺口宽限；ACK 语义；AI 仍按 tick 走 | `go test ./...` 全绿，含突变验证 |
| C# 仿真 | 序号锚定和解、冗余窗口、40 个 seeded 受损链路场景、8/16/24 人 soak、渲染插值/接缝/均匀性 | `Starve.Netcode.Tests` 161 全绿；`Starve.Core.Tests` 249 |
| 端到端（真 gate + 无头客户端） | 8 档上行受损（0~300ms 延迟、抖动、丢 30~40%、乱序 30%） | 4 轮连续 8/8；每档 `锚定命中=400/400`、输入延迟 ≈1 tick |
| 真客户端逐帧 | 走路表现（位移→moving→动画）+ 同跨度位移对比 | 相邻帧速度差 5.29 → 0.02~0.04 格/秒；`offsetDist` 恒在一个 tick 内 |

---

## 7. 已知残余

1. **`EffectiveSpeed` 变化的瞬态**：有效速度（饥饿/负重/效果）只能从快照得知，客户端在被告知前的那 1~2 步会按旧速度走（实测偏差 = 速度变化 × 1 tick，0.19~0.25 格，1~2 步自愈）。彻底消除要把"速度变化"当成一次输入事件、把尚未确认的步用新速度重算。
2. **建筑（盒形占位物）与附近生物的避让**：本地滑动/ORCA 与服务端求解有细微差别（A/B：只喂盒 12%、只喂邻居 24% 校正），属模型细节，偏差有界。
3. **无头门禁的模型可比性**：测试台没有服务端的全局碰撞索引/地形高度/AI，干净链路的"直接贴"阈值必须留模型容差；严格判据落在"与地形无关"的那组（锚定命中、流吃干净、无风暴、末尾收敛、输入延迟）。
