# P1.5 客户端预测 / 服务端和解 / 外推 —— 设计

> 本文是实现前的设计（不含代码）。范围由你定：
>
> 1. **实现**客户端预测 —— 本地立刻动，不等往返；
> 2. **实现**服务端权威数据到达后的**校验 + 和解**；
> 3. **实现**客户端外推（样本迟到时按速度补）；
> 4. **实现**未确认输入的重放 —— 但**只在本地状态里重放**，
>    不重放渲染、不重放游戏逻辑，只用来得出"服务端校正后应有的最新位置"；
> 5. **不实现**延迟补偿（lag compensation）；
> 6. 客户端已经有与服务端**同一套移动 + 碰撞算法**（`OwnMovementSim` ↔
>    `MoveSolver` + `ApplyDisplacement`，由 `movement_golden.json` 守契约）——本次复用它，不另写。

---

## 0. 现状：为什么还要做这一步

float 插值时钟（手册第 11 节）修完之后，剩下的顿挫有两个代码级来源，都还没被实测证实：

| # | 问题 | 位置 | 后果 |
| --- | --- | --- | --- |
| P1 | **拿"本地现在"比"服务端过去"** | `OwnMovementSim.ReconcileCore` 的 `err` | 差值含网络领先量 `v×(往返/2 + 快照间隔)`；10 格/秒 + 100ms 就是 1.0 格 > 0.75 阈值 → **每个快照都可能触发一次 20% 回拉**，形成 20Hz 锯齿 |
| P2 | **停止 = 一次到位吸附** | `BlendTo(..., serverStopped ? 1f : k)` | 松键时按 `v×领先` 硬跳一次；角色站在高度场上 → 横向跳必然带来**上下跳** |
| P3 | 权威速度不是"实际接受位移" | 服务端 `move_system.go:107` 早于 `:111` | 贴岸/被挡时 `vel=10` 而位移 0 → 外推朝不可走方向走，下一帧再拉回（橡皮筋） |
| P4 | 没有输入历史 | `OwnMovementSim` 只存当前 `_dirX/_dirY` | 无法区分"领先"与"预测错了"，阈值只能折中，必有一侧误判 |

P1 是主因，P2 是"上下卡"的直接来源。**解法就是文献里的标准做法**（见附录 A）：
给输入编号、服务端回"已处理到第几号"、客户端**从权威状态重放未确认输入**。

---

## 1. 三条数据流（先把名词定死）

| 对象 | 我们怎么得到它的位置 | 时间上在哪 | 走哪条路 |
| --- | --- | --- | --- |
| **自己** | 本地跑同一套移动/碰撞算法 | **现在** | 预测 → 和解放 → **重放** |
| **其他玩家 / 生物** | 只用服务端下发的真实位置，不猜 | **过去**（延迟 `delayTicks`） | **实体插值**（+ 有界外推） |
| **时间基准** | 墙钟 ↔ 服务端 tick 的稳定映射 | — | `ServerClock` |

一句话：**自己看现在，别人看过去。** 这不是 bug，是这套架构的固有取舍
（Gambetta FPM III：把别人渲染在过去，换来完全平滑、绝不瞬移）。

---

## 2. 自己：预测 → 校验 → 重放

### 2.1 核心设计：把"推进"统一成"推进到某个 tick"

现在是两种推进混在一起（每帧 `Tick(deltaMs)` + 和解时 `BlendTo`），
于是"重放"和"正常往前走"成了两条代码路径，时间容易被消费两次。

改成**只有一个推进函数**：

```csharp
// 纯状态推进：把本地推到 targetTick（服务端时间轴上的时刻）
// 内部按 50ms 切片，逐 tick 从输入缓冲取"那个 tick 有效的意图"
private void AdvanceTo(double targetTick, InputBuffer inputs)
{
    while (_advancedTick + 1 <= targetTick)
    {
        _advancedTick += 1;
        var intent = inputs.IntentAtTick(_advancedTick);   // 按住方向 → 取当时那条记录
        StepOneTick(intent);                                // 与服务端同一套数学
    }
}
```

于是两条路变成同一句调用：

```csharp
// 正常帧：只是往前推
AdvanceTo(clock.TickAt(now), inputs);

// 收到快照：先回到权威状态，再往前推（= 重放）
Rebase(srvX, srvY, srvTick);      // 锚点/子格 = 权威连续位置
AdvanceTo(clock.TickAt(now), inputs);
```

**"重放"就是"从权威状态重新 AdvanceTo 到现在"** —— 没有第二套代码，
也就不存在"重放和正常推进不一致"的可能。

### 2.2 收到的权威数据要带什么

| 字段 | 现状 | 用途 |
| --- | --- | --- |
| `Position.X/Y` + `Moveable.SubX/SubY` | 有 | 权威**连续位置**（`Position + Sub`），Rebase 的输入 |
| 快照 `tick` | 有 | 这次权威状态对应服务端哪个时刻；重放的起点 |
| `last_accepted_seq` | 有 | 服务端"收到了"到第几号 |
| **`last_applied_seq`** | **缺，要加** | 服务端"**已经烘进这份位置**"到第几号。没有它，重放起点是猜的，`err` 归不了零 |
| `VelX/VelY` | 有但会撒谎 | 外推用；**必须改成"实际接受位移/dt"**（见 §5） |

### 2.3 输入缓冲

```csharp
readonly record struct InputRecord(
    ulong Seq, ulong Epoch, int Dx, int Dy, long SentWallMs);

sealed class InputBuffer
{
    void Record(InputRecord r);            // 只在意图变化时记（按住方向 → 每秒几条）
    void Acknowledge(ulong appliedSeq);    // 丢弃 <= appliedSeq 的记录
    (int Dx, int Dy) IntentAtTick(long tick);  // 重放时按 tick 查"当时那条"
}
```

- `IntentAtTick(t)` = 最后一条"生效时刻 ≤ t"的记录；`Seq` 用于和解后裁剪。
- 记录**在发送时**写入（与 `Commands.Move` 同源），保证"发出去的"和"重放用的"是同一份。

### 2.4 和解判据：只在"真不一致"时才校正

重放之后 `err` 的含义变了 —— 它不再含网络领先量，**它才是真失配**：

```
replayed = AdvanceTo(now) 的结果（从权威状态出发 + 重放未确认输入）
err      = |replayed - 校正前显示的位置|
```

| `err` | 处理 |
| --- | --- |
| `< 死区`（建议 0.02~0.05 格） | 忽略。这是 ORCA/浮点噪声，不是错 |
| 死区 ~ 传送阈值（4 格） | 采用 `replayed` 作为模拟状态。**先直接采用**；如果日志显示仍可见，再加"渲染偏移衰减"（见 §2.6） |
| `> 4 格` | 真瞬移：直接贴合 + 清偏移 |

`serverStopped` 的**特例删掉** —— 停止只是"重放结果 == 权威"的普通情形，
不再需要 `BlendTo(..., 1f)`，也就没有松键那一跳（P2）。

### 2.5 无副作用契约（对应你第 4 点）

重放**只在 `OwnMovementSim` 里跑纯运动学**，只产出一个最终位置。明确边界：

| 重放**可以**改 | 重放**绝不能**碰 |
| --- | --- |
| 锚点 / 子格（`_anchorX/_subX`） | 渲染节点、相机 |
| 当前速度 `_velX/_velY`（ORCA 的输入） | 动画播放/切换、脚步音效、VFX |
| 推进游标 `_advancedTick` | HUD、动作取消、伤害判定、任何命令发送、任何世界状态 |

现状检查：`OwnMovementSim.Tick` 本身已经是纯运动学（无副作用），
侧效都在 `GameRoot` 的渲染/输入分支里 —— **只要不把这些搬进重放路径即可**。
渲染层照常每帧读一次 `sim.Position`，完全看不到"刚刚重放了几 tick"。

### 2.6 可选层：渲染偏移衰减（不是重放）

如果实测发现重放残差仍可见，再加这一层（**纯表现层，不反馈进模拟**）：

```csharp
offset += (旧显示位置 - replayed);     // 只记残差
offset *= exp(-dtMs / 100);            // 约 100ms 收敛到 0
renderPos = sim.Position + offset;     // 相机也跟这个
```

它**不是**重放、也不改游戏逻辑；没有它时行为是"直接采用重放结果"。
先用简单版上线，用日志判断要不要它。

---

## 3. 别人：实体插值（这一节是"是什么、为什么"）

### 3.1 问题

服务端**固定 20Hz** 广播状态，客户端渲染 60~120Hz。若"收到包就更新位置"，
别人的动作就是每秒 20 次小瞬移 —— 看起来一顿一顿。

### 3.2 两种解法

| 解法 | 做法 | 什么时候能用 |
| --- | --- | --- |
| **外推 / dead reckoning** | 用已知速度猜**未来**，收到新包再纠正 | 只适合运动可预测的：赛车、飞船。Fiedler：**一旦物体与非静止物体碰撞，外推就开始失效** |
| **实体插值** | 在**过去**的两个已知位置之间插值 | 通用；转向/急停频繁时唯一可靠的选择 |

我们的生物是行为树驱动 + ORCA 软避让 + 地形碰撞 —— 方向随时可反转，
属于"外推必失效"那一类。所以**别人走插值，不走 dead reckoning**。

### 3.3 实体插值怎么做（具体到我们的数）

1. **缓冲**：把每个带 `tick` 的快照存进插值器（`PositionSmoother` 的 `_samples` 已经在做）。
2. **播放时钟**：渲染时不要用"最新 tick"，而是用**落后固定延迟**的时刻：

   ```
   playoutTick = ServerClock.TickAt(now) - delayTicks
   ```

3. **插值**：找到 `playoutTick` 落在哪两个样本之间，在这两点之间插值（现在用线性；
   若下发速度可信，可升级 Hermite，见附录 A 修订 2）。
4. **为什么必须"落后"**：插值需要**下一个**样本才能往它走 —— 没有未来样本就插不了。
   所以这段延迟是**结构性的**，不是实现缺陷。`delayTicks` 就是拿延迟换平滑的旋钮。

### 3.4 延迟取多大 / 不够会怎样

- 现状 `delayTicks = 1`（= 50ms = 1 倍发包间隔），**余量为零**：
  只要样本间隔超过 `50ms + 一帧`（60FPS 要 >66.7ms，120FPS 要 >58.3ms），
  播放时钟就跑过最后一个样本 → 没东西可插了。
- 这时只有两条烂路：**冻结在端点**（一帧不动）或 **外推**（猜）。Fiedler 的经验：
  缓冲取 **3 倍发包间隔**（20Hz → 150ms），为的是"连丢两个包仍有插值目标"。
- → 建议 `delayTicks` 提到 **2~3**，并做成按抖动分位数自适应。
  代价：别人多 50~100ms 的旧（FPM III：100ms 一般不察觉）；自己不受影响（自己是预测）。

### 3.5 外推（你要求实现的那条）

外推只用在**一个地方**：播放时钟越过了最新样本（快照迟到/丢了）。
规则：

- **有界**：最多外推 `maxExtrapTicks`（建议 1~2 tick），到顶就**冻结端点**，
  绝不"先冲出去再倒退"（那是最难看的橡皮筋）；
- **速度来源**：优先用服务端权威速度（含坡度/ORCA 结果）；但必须先修 §5 的第 1 项，
  否则贴岸时会朝墙里外推。保守做法：权威速度与"最近两样本推出的速度"
  取较小者（或夹在 1.5 倍以内）；
- **服务端确认停止**（速度清零 + 无路径）时**不外推**，直接停在最后位置；
- **自己不走外推** —— 自己在跑预测，不存在"样本迟到"。

### 3.6 这套设计的一个已知代价（不做延迟补偿的后果）

别人被渲染在过去 ⇒ 你瞄准的是他 100ms 前的位置。对**战斗判定**意味着：
客户端基于"过去的位置"预测命中，服务端基于"现在"校验，两边可能不一致。
因为我们明确不做延迟补偿，缓解办法是**不要把远程实体的战斗结果当权威来表现**：

- 本地只预测自己的挥击动画 / 音效；
- 命中反馈（血花/受击数字）等 `Impact` 事件回来再播；
- 出现"预测命中但服务端没判中"时，静默纠正，不弹提示。

（`ImpactPresentationController` 现在的预测命中就是在打这个擦边球，值得按上面收一收。）

---

## 4. 时间基准：`ServerClock`

```csharp
sealed class ServerClock
{
    void OnSnapshot(long serverTick, long nowMs);  // 慢速校正，不跳
    double TickAt(long nowMs);                      // 当前服务端 tick（含小数）
    double LeadTicks(long nowMs, long snapshotTick);
}
```

- 每个快照算出观测偏移 `observed = now - serverTick*50`；
- 每帧让 `offset` 朝 `observed` 最多移动 `MaxStepMs`（建议 2ms），**不跳变**；
- 偏差超过 `ResyncMs`（建议 250ms，卡顿/重连）才硬同步。

用途：① 插值播放时钟；② 重放的终点（`AdvanceTo(TickAt(now))`）。
治的是"每个样本把插值时钟重锚一次"造成的相位误差（实测 349 帧里 7 帧异常）。
纯逻辑、可单测（注入合成时间戳）。

---

## 5. 需要服务端配合的（就两项）

1. **`VelX/VelY` 改成"已被格子层接受的位移 ÷ dt"**。
   现在 `move_system.go:107` 在 `ApplyDisplacement`（`:111`）**之前**就把求解器速度写进组件，
   而格子层可能整段拒收（`stepAxis` 把 sub 钳在 0.999/0.001，本 tick 零位移）。
   实测：贴岸时 `sub` 钉在 0.9990 而 `vel=(10.000, 0.000)`。
   受害方：客户端外推（朝墙里走）、客户端 ORCA 邻居速度（对"其实停着"的实体做错误避让）。
2. **快照补 `last_applied_seq`**（已有 `last_accepted_seq`）。
   FPM II 的算法要求服务端回"**已处理到的输入序号**"；"已接受"≠"已烘进这份位置"。
   有了它，重放起点就是确定的，`err` 才可能真的归零。

两项都小，建议先做 —— 它们是重放和外推正确性的**前置**。

---

## 6. 验收与观测

单元测试（`Starve.Core.Tests`）：

- 已有 `ReconcileTraceTests`：**同一 tick 跨度**的速度比较（与延迟无关）、decision 分类、
  "服务端声明速度 vs 实际位移"可分辨；
- 新增：`AdvanceTo` 重放确定性 —— 给定录制的意图流 + 权威位置流，重放结果与权威一致（1e-3）；
- 新增：**重放不产生副作用** —— 重放前后，除位置/速度外无任何状态变化；
- 新增：直线行走 60 秒，校正次数（Soft/Hard）为 0；
- 新增：外推有界、到顶冻结、绝不倒退；
- 新增：停止不瞬移（单帧位移上界）。

在线判据（`STARVE_MOVE_TRACE=1`，列已在上一轮加好）：

- `driftX/driftY/driftSpeed` 直线段 ≈ 0；
- `decision` 直线段全 `NoError`；
- `dRendY` 无孤立尖峰（否则是横向回拉带出来的上下跳）；
- `srvStepSpeed` 与 `srvVelSpeed` 一致（否则服务端第 1 项没修）。

---

## 7. 分期

| 阶段 | 内容 | 可回退开关 |
| --- | --- | --- |
| **0 ✅** | 观测：`MoveTrace` + `LastReconcile` + `ReconcileTraceTests` | `STARVE_MOVE_TRACE=1` |
| **1** | 服务端两项（§5） | — |
| **2** | `ServerClock` + 插值改用它（不改和解行为） | `STARVE_RECON_CLOCK=1` |
| **3** | `InputBuffer` + `AdvanceTo` 统一推进（先把"正常推进"切过去，行为等价） | 内置自检 |
| **4** | 和解改走"Rebase + AdvanceTo"；删 `StopSnap` 特例 | `STARVE_RECON_MODE=replay` |
| **5** | （按日志决定）插值缓冲加大 / Hermite / 渲染偏移衰减 | 独立开关 |

第 3 步是关键的安全动作：**先把"推进"统一到 tick 时间轴且行为等价**，
第 4 步再换和解路径，这样出问题能立刻定位到是"推进"还是"和解"。

---

## 附录 A：文献对照（Fast-Paced Multiplayer / Snapshot Interpolation）

参考：[FPM I 架构](https://www.gabrielgambetta.com/client-server-game-architecture.html) /
[II 预测与和解](https://www.gabrielgambetta.com/client-side-prediction-server-reconciliation.html) /
[III 实体插值](https://www.gabrielgambetta.com/entity-interpolation.html) /
[IV 延迟补偿](https://www.gabrielgambetta.com/lag-compensation.html) /
Fiedler [网络演进与预测](https://gafferongames.com/post/what_every_programmer_needs_to_know_about_game_networking/) ·
[Snapshot Interpolation](https://gafferongames.com/post/snapshot_interpolation/)

### A.1 与本设计一致的部分

| 文献结论 | 本文对应 |
| --- | --- |
| "客户端看到现在，服务端状态是过去" | §0 P1 —— 架构固有性质，不是实现细节 |
| "不要直接把权威状态贴上去（等于把客户端拽回过去）；要留环形缓冲、丢弃已确认、**从权威状态重放未确认输入**" | §2.1 `Rebase + AdvanceTo` |
| 服务端回复要带"已处理到的输入序号" | §5 第 2 项（`last_applied_seq`） |
| 多人下"预测与权威不一致"是正常现象（别人挡住你） | §2.4 的死区 |
| 自己现在、别人过去；100ms 一般不察觉 | §3 |
| 外推在碰撞/非静止物体场景会失效 | §3.5 外推降级为最后手段 |

### A.2 文献带来、但本设计暂不做的

- **快照走 UDP**（Fiedler：时间敏感数据不该走 TCP）。我们现在是 WebSocket/TCP，
  队头阻塞会把一次延迟放大成一次可见顿挫。改传输层是大工程，先靠缓冲余量缓解。
- **延迟补偿**：按你的要求不做（§3.6 是其代价与缓解）。
- **Hermite 插值**：Fiedler 指出线性插值在样本点**速度不连续**（折角），
  而我们**已经在快照里下发 `VelX/VelY`**，具备升级条件。放在阶段 5，按日志决定。

### A.3 一条我推错过的结论（留档，别再走）

我一度断言"实测约一半的样本间隔 >50ms，所以外推经常发生"——**错的**。
外推不是"间隔 > 50ms"就触发：`dt = _latestTick + sinceUpdate/50 - 1`，
需要 `sinceUpdate > 50`；而 `sinceUpdate` 最大只能是"上次应用快照 → 下一帧" =
`间隔 - 一帧`。所以真正条件是 **间隔 > 50ms + 一帧**（60FPS >66.7ms，120FPS >58.3ms）。
实测局域网间隔 47.8~51.5ms → 当前**不触发**，与日志 `外推帧=0` 一致。
结论：加大缓冲是**买余量**（抗 WAN 抖动/TCP 队头阻塞/帧卡顿），不是当前症状的解药。
