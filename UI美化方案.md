# UI 美化方案

> 状态：执行中（2026-08-21）  
> 范围：`GodotClient` 的 HUD / 小地图 / 交互反馈观感  
> 不改：协议、移动预测、权威动作契约、世界渲染管线（光照 / LUT / 天气）

---

## 0. 结论先行

当前「丑」主要来自 **UI 原型感**，不是渲染管线缺能力：

| 层 | 现状 | 观感影响 |
| --- | --- | --- |
| 后处理 / 昼夜 / 天气 | 已 polish | 世界有氛围 |
| 地形 / 主角 | 有真资产 | 局部好看 |
| HUD | 默认 Button / Label / ProgressBar，左右满屏调试布局 | **立刻显工具面板** |
| 小地图 | 裸 TextureRect，无边框/标题 | 像调试贴图 |
| 普通实体 | 大量菱形占位 | 近景毁氛围（本方案外，另立项） |

本方案目标：把 HUD 从「可玩调试面板」做成 **焦木 / 青铜 / 旧金** 哥特生存风的游戏级 UI，且不破坏 P0.1 增量刷新契约。

---

## 1. 目标与非目标

### 目标

1. 第一眼是游戏 HUD，不是工程师面板。
2. 视觉语言统一：暗木面板、青铜描边、旧金高亮、低饱和正文。
3. 布局按「生存游戏」习惯：生命与状态靠角、操作与背包靠底、制作可开合、日志不抢戏。
4. 保持现有公开 API（`SetStatus` / `SetVitals` / `RenderInventory` / `RenderCraft` / 事件），`GameRoot` 接线尽量不动。
5. 继续遵守：背包 / 配方只在签名变化时重建；相同文本不重复写入。

### 非目标（本方案不做）

- 重写光照 / LUT / Bloom / 天气
- 替换菱形实体占位（另开「实体视觉补齐」）
- 水面波纹、草簇等世界填充
- 新玩法或协议字段
- 完整音效包（可留钩子，不阻塞 UI）

---

## 2. 视觉方向

沿用迁移文档已定调性，落地为可编码的令牌：

| 令牌 | 用途 | 建议色 |
| --- | --- | --- |
| `Ink` / 面板 | 深色玻璃底 | `#101214` / `#16181C` |
| `GoldOld` / 正文 | 选中与文字 | `#E8E0D4` / `#F2EEE6` |
| `Blood` | 生命圆环，始终红 | `#E24B4B` |
| `HungerFill` | 饥饿圆环，始终琥珀 | `#E8A033` |
| `Spirit` | 灵魂态 | `#7EC8E8` |

原则：

- **少卡片**：面板有边框即可，不要多层阴影堆叠。
- **少文字按钮行**：动作改为底栏图标槽；建造 / 睡眠进次级面板或长按菜单（P2）。
- **字体**：HUD 使用 `assets/ui/fonts/SmileySans-Oblique.ttf`（得意黑，OFL）。
- **对比度**：夜景 LUT 下 UI 仍可读；面板 alpha 约 `0.92`，不要全透明糊在世界上。

---

## 3. 信息架构（布局）

从「左右两列 420px 调试墙」改为锚点式 HUD：

```
┌──────────────────────────────────────────────┐
│ 生命条 / 灵魂态          FPS(调试)   小地图  │
│ 短状态一行                                   │
│                                              │
│                                              │
│                                              │
│                                              │
│  [制作抽屉 ◀]                                │
│                                              │
│ ════════ 底栏：动作槽 + 背包格 ════════════ │
│ 日志：底部一条可展开，默认 2~3 行            │
└──────────────────────────────────────────────┘
```

| 区域 | 内容 | 交互 |
| --- | --- | --- |
| 左上 | 生命条 + 文案（含灵魂态） | 只读 |
| 左上偏下 | 一行状态（选中目标 / 提示） | 只读 |
| 右上 | 小地图（带木框标题） | 只读（后续可点） |
| 右侧 | 制作面板，默认收起，按钮展开 | 开合 |
| 底部 | 动作快捷槽 + 背包格 + 使用/装备/丢弃/拆分 | 主交互 |
| 底上 | 战斗日志，默认矮，可展开 | 只读 |

调试信息（FPS、预测 epoch 等）保持可开关，默认右上小字，不进主视觉层级。

---

## 4. 分阶段交付

### P0 · Theme 与令牌（0.5~1 天）✅ 已落地

- 新增 `HudTheme`：集中色板、`StyleBoxFlat`、Theme 工厂。
- `Hud` 统一 `Theme = HudTheme.Create()`，左右列外包焦木 `PanelContainer`。
- 背包槽支持选中金框；生命色走令牌。
- 布局逻辑暂未改（留给 P1）。

**验收**：截图中按钮 / 进度条 / 面板边框已是焦木青铜，不再是默认灰。

### P1 · 布局重排（1~2 天）✅ 已落地

- `Hud` 改为锚点区域：左上生命、底栏行动 + 短日志。
- **背包 / 制作均为点击抽屉**（底栏「背包」「制作」按钮；快捷键 B/I、C；Esc 关闭）。
- 窗口：`1920×1080` 设计分辨率、启动最大化、`canvas_items` + `expand` 随窗口拉伸。
- 小地图右上角锚点 + 焦木边框，随窗口贴边。
- 中间大片世界可点穿（根节点 `MouseFilter=Ignore`）。
- 公开方法与事件签名保持不变。

**验收**：默认不挡视野；点「背包」「制作」可弹出完整面板。

### P2 · 控件语义化（2~3 天）✅ 已落地

- ✅ 背包 / 制作：点击弹出抽屉（本迭代已做）。
- ✅ 背包格：固定槽位皮肤、数量角标、装备角标、选中金框。
- ✅ 动作栏：单字图标槽 + Tooltip（真图标留给 P4）。
- ✅ 制作列表：卡片行（产出名、材料进度、工作站标签）。
- ✅ 生命条：自绘 10 段生命 + 饥饿条，灵魂态描边。

**验收**：无需读「一排中文按钮」也能完成常用操作；制作失败原因仍可见。

### P3 · 反馈与抛光（1~2 天）

- Toast / 短提示与日志分层（重要提示上浮，刷屏进日志）。
- 受击红屏与 HUD 生命条色调统一。
- 禁用态、悬停、按下三态齐全。
- 可选：简易面板开合缓动（200ms）。

**验收**：死亡灵魂态、制作中、无材料「尝试制作」三种状态一眼可辨。

### P4 · 资产与验收固化（1 天）

- [x] 接入字体文件到 `GodotClient/assets/ui/fonts/`（得意黑）。
- 必要的九宫格面板 / 槽位框（可先 Flat，后换贴图）。
- 更新 README「当前状态」与截图验收清单。
- `make check` + F5 手工清单全过。

---

## 5. 文件清单

| 路径 | 动作 | 说明 |
| --- | --- | --- |
| `UI美化方案.md` | 新增 | 本文档 |
| `GodotClient/Game/HudTheme.cs` | 新增 | 色板 + Theme 工厂 |
| `GodotClient/Game/Hud.cs` | 改 | 应用 Theme；P1 起改布局 |
| `GodotClient/Game/VitalBar.cs` | 新增 | P2：分段生命 / 饥饿条 |
| `GodotClient/Game/InventorySlot.cs` | 新增 | P2：背包固定槽 |
| `GodotClient/Game/ActionSlot.cs` | 新增 | P2：底栏动作槽 |
| `GodotClient/Game/MinimapView.cs` | 改 | P1：边框 / 锚点与 HUD 对齐 |
| `GodotClient/Game/DamageFlashOverlay.cs` | 小改 | P3：色调与 Blood 令牌对齐 |
| `GodotClient/assets/ui/` | 新增目录 | 字体、九宫格、图标（P2/P4） |
| `README.md` | 小改 | P4：标记 UI 美化阶段 |

**进度**

| 阶段 | 状态 |
| --- | --- |
| P0 Theme 与令牌 | ✅ 已落地（`HudTheme` + `Hud` 换皮） |
| P1 布局重排 | ✅ 已落地（锚点 HUD + 背包/制作抽屉） |
| P2 控件语义化 | ✅ 生命条 / 动作槽 / 背包槽 / 制作卡片 |
| P3 反馈与抛光 | ⬜ |
| P4 资产与验收 | ⬜ |

---

## 6. 约束（必须遵守）

来自既有基线，美化不得违反：

1. **P0.1**：HUD 只在背包、装备、配方、工作站或制作进度分段变化时重建子控件。
2. **P0.3**：相同状态文本不重复写入。
3. 配方按钮始终可点；材料不足显示「尝试制作」，以服务端错误为准。
4. 死亡时禁用采集 / 攻击 / 砍伐 / 挖掘 / 拾取 / 制作；生命 / 灵魂文案仍由 `HudVitalsViewModel` 驱动。
5. 纯 UI 层：不在 `Hud` 里发协议、不算玩法规则。

---

## 7. 验收清单（手工）

- [ ] 默认灰控件基本消失，面板为焦木 + 青铜描边。
- [ ] 生命条 / 灵魂态在左上清晰可读。
- [ ] 底栏可完成：选格 → 使用 / 装备 / 丢弃 / 拆分。
- [ ] 制作面板可展开，材料不足可点「尝试制作」并看到失败原因进日志。
- [ ] 死亡：灵魂文案 + 交互禁用；复活后恢复。
- [ ] 行走时打开制作面板，帧率不因每 tick 重建崩溃（签名刷新仍有效）。
- [ ] 1280×720 与全屏下布局不溢出、不挡角色脚底过久。

自动化：

```bash
dotnet build GodotClient/GodotClient.csproj
dotnet test Starve.Core.Tests/Starve.Core.Tests.csproj
make check
```

---

## 8. 执行顺序（本仓库）

1. **现在**：落地 P0（`HudTheme` + `Hud` 换皮）。
2. 接着 P1 布局重排（同一 PR 或紧随 PR）。
3. P2 / P3 按控件拆 commit，便于回滚观感。
4. 实体菱形占位、水面草簇 → 独立方案，不塞进本清单。

---

## 9. 风险

| 风险 | 缓解 |
| --- | --- |
| 布局大改破坏点击热区 | 先 Theme 后布局；每阶段截图对比 |
| 程序化 StyleBox 仍显「扁」 | P4 换九宫格贴图，API 已用 Theme 则只换资源 |
| 字体版权 | 选用可商用字体或系统回退，写入 `assets/ui/fonts/README` |
| 与 web 旧 CSS 不完全一致 | 以「游戏可读 + 焦木青铜」为准，不追求像素级复刻 |
